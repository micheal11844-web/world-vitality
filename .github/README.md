# .github/

GitHub-native repo configuration: CODEOWNERS, pull request template, and CI workflows (`workflows/`).

Branch protection itself is a GitHub repo setting, not a file — see `docs/onboarding/repository-setup.md` for the exact settings to apply once this repo exists on GitHub.
