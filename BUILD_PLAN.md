# WORLD VITALITY — BUILD PLAN

**Type:** Execution artifact, not a foundational document. This file is expected to change constantly — check items off, reorder, add tickets — without needing approval as if it were the Constitution, Engineering Blueprint, PRD, or Experience Blueprint.
**Purpose:** Translate the Engineering Blueprint's Section 18 roadmap into concrete, sequenced, checkable tickets, so execution work (in Claude Code or elsewhere) doesn't need to re-derive intent from the four foundational documents every session.
**Operating constraint:** No local terminal / command-line workflow, no VS Code. All code changes land via a GitHub-integrated tool; Vercel deploys automatically from GitHub on merge — never a manual build/deploy step.

---

## HOW TO USE THIS FILE

Each ticket below is scoped to be independently completable and reviewable. Work top to bottom — later tickets assume earlier ones are done. Check off `[ ]` → `[x]` as completed. If a ticket turns out to need splitting, split it here rather than starting a new planning document.

---

## STAGE 0 — REPOSITORY FOUNDATION

- [x] **0.1** Initialize the `world-vitality` GitHub repository (monorepo, per Engineering Blueprint Section 1).
- [x] **0.2** Create the top-level folder skeleton exactly as specified in Engineering Blueprint Section 2 (`apps/`, `services/`, `packages/`, `infra/`, `docs/`, `tools/`, `tests/`, `.github/`), with a placeholder `README.md` in each explaining its purpose (one paragraph, copied/adapted from the Blueprint's rationale).
- [x] **0.3** Move the four locked foundational documents into `docs/` (`docs/constitution/`, `docs/engineering-blueprint/`) and this `BUILD_PLAN.md` into the repo root.
- [x] **0.4** Set up `.github/CODEOWNERS`, `PULL_REQUEST_TEMPLATE.md`, and branch protection on `main` (required review, required passing checks, no force-push) — per Engineering Blueprint Section 1.5 and 13.
- [x] **0.5** Connect the repository to Vercel for automatic deploy-on-merge to a staging environment; confirm a trivial change (e.g., README edit) triggers a successful deploy end-to-end before writing any application code.
- [x] **0.6** Set up linting/formatting/TypeScript configuration presets in `packages/config/`, applied repo-wide.
- [x] **0.7** Write ADR-0001 (Monorepo decision) and ADR-0002 (Ingestion/Interpretation/Presentation separation) into `docs/adr/`, formalizing decisions already made in the Engineering Blueprint, per its ADR format (Section 5).

## STAGE 1 — CORE ABSTRACTIONS (before any feature work)

- [x] **1.1** Define the internal data-ingestion interface (`services/data-ingestion/`) — the contract any provider connector must implement. No provider wired in yet.
- [x] **1.2** Define the internal AI/interpretation-provider interface (`services/interpretation-engine/`) — the contract any AI model adapter must implement.
- [x] **1.3** Define the shared data schema (`packages/data-schemas/`) that ingestion normalizes into and interpretation consumes from.
- [x] **1.4** Write ADR-0003 documenting both interfaces and why they exist (protects provider-agnosticism per Constitution Section 4).

## STAGE 2 — FIRST DATA PROVIDER + DATA PROVENANCE

- [x] **2.1** Implement the first concrete ingestion connector (NASA) against the Stage 1 interface.
- [x] **2.2** Document data provenance and licensing for this source in `docs/data-provenance/` (Constitution Section 24 requirement — not optional).
- [x] **2.3** Validate the ingestion → schema pipeline with real data end-to-end (no interpretation logic yet).

## STAGE 3 — IDENTITY & ACCESS FOUNDATION

- [x] **3.1** Implement authentication (magic link + optional SSO groundwork) via `services/identity-service/`.
- [x] **3.2** Implement the core permission-role model shared across workspaces (Admin/Owner, Operational User, Scoped/Field User, Viewer/External — per PRD Section C.2).
- [x] **3.3** Implement account settings basics: profile, data export, account deletion (Constitution Privacy Principles — must exist before any real user data accumulates).

## STAGE 4 — INTERPRETATION ENGINE v1 (NARROW SCOPE)

- [x] **4.1** Build one deliberately narrow interpretive capability end-to-end (recommend: Agriculture soil-moisture status — plain-language insight + confidence level) using the Stage 1 AI interface.
- [x] **4.2** Build the evaluation framework (`packages/ai-evaluation/`) and validate this first capability against ground truth before it's user-facing.
- [x] **4.3** Build the shared "confidence/uncertainty" design-language component (per Experience Blueprint recommendation, Section 20 of the Experience Blueprint) in `packages/ui-components/` — used everywhere from day one, not retrofitted later.

## STAGE 5 — DESIGN SYSTEM FOUNDATION

- [x] **5.1** Implement design tokens (`packages/design-tokens/`) — color, typography, spacing — per Experience Blueprint Section 13 principles (no hardcoded values in components).
- [x] **5.2** Implement core shared components in `packages/ui-components/`: Button, Card, Input, Modal, Typography, Table, Empty/Loading/Error/Success states, Skeleton loader.
- [x] **5.3** Implement dark mode / light mode support as first-class from the start.
- [x] **5.4** Implement the app-shell layout components (Header, Sidebar, AI Panel dock) per the Experience Blueprint's wireframe (Section 4 app shell).

## STAGE 6 — FIRST APPLICATION SURFACE

- [x] **6.1** Build `apps/web` skeleton: routing structure, app shell wired to design system, authentication flow wired to `identity-service`.
- [x] **6.2** Build the Home Dashboard shell (cross-workspace landing view, per PRD Section B.3).
- [x] **6.3** Build one full Workspace Home (recommend: Agriculture) using the dashboard widget grammar from Experience Blueprint Section 9 and its wireframe.
- [x] **6.4** Build the Map view (base layers + one data overlay) per Experience Blueprint Section 11.
- [x] **6.5** Wire the AI Panel to the Stage 4 interpretation capability, including confidence display.

## STAGE 7 — OBSERVABILITY, SECURITY, ACCESSIBILITY GATES

- [x] **7.1** Implement logging/monitoring/alerting basics (Engineering Blueprint Section 12) alongside Stage 6 — not after.
- [x] **7.2** Run a security review pass on the full pipeline built so far (Engineering Blueprint Section 14).
- [x] **7.3** Run an accessibility pass against Stage 5/6 components (Experience Blueprint Section 15) before any public exposure.
  - **Follow-up gap, now closed:** `apps/web` had no Next.js-specific ESLint rules (image/font/script/head misuse, etc.) — the shared repo-wide config in `packages/config/eslint.config.js` only covered generic JS/TS/React-hooks/a11y rules, not anything Next-specific. Closed by adding `@next/eslint-plugin-next`'s native flat-config `core-web-vitals` rule set, scoped to `apps/web/**` in the root `eslint.config.js`. Deliberate substitution, flagged: did not use the `eslint-config-next` package itself, since as of Next 15.5 it still only ships legacy `.eslintrc`-format config and would need the `@eslint/eslintrc` `FlatCompat` shim as an extra dependency to bridge into this repo's flat-config setup — `@next/eslint-plugin-next`'s own flat export gives the identical rule set with one less moving part. Verified with a full repo-wide `eslint .` run (exit 0, no errors).

## STAGE 8 — CONTROLLED RELEASE

- [x] **8.1** Deploy to an internal release channel first (Engineering Blueprint Section 13 staged rollout).
  - **Note on actual approach taken:** rather than a dedicated internal-only channel (separate branch + password protection), this was satisfied by cutting a versioned release directly on `main` (package.json version bump, GitHub Actions rebuild, GitHub Release published) — reusing an existing release workflow already in use on another project. This deploys straight to the same production URL rather than a gated internal one. Flagged deliberately, not silently substituted: for a solo-owned project where the owner is currently the only user, this is a reasonable simplification of the letter of the ticket, not a shortcut around its intent (catching problems before wider exposure) — there was no wider exposure yet to guard against.
- [ ] **8.2** Expand to limited beta once internal validation passes.
  - **Status: rescoped by the owner.** No longer treated as a gate blocking further feature work. Owner (solo, project not yet shown to anyone) decided to keep building — Guide Character, then Weather & Climate — and will run the actual beta once the app feels complete, rather than pausing feature work to find testers now. 8.2 and 8.3 both wait until that point.
- [ ] **8.3** Retrospective: update this BUILD_PLAN with Stage 9+ tickets based on what was actually learned — do not pre-plan Stage 9 in detail until Stage 8 is real.
  - **Note:** Stages 9 and 10 below were started before this retrospective, at the owner's explicit direction — a deliberate deviation from this ticket's own sequencing rule, recorded rather than silently reordered. See v5/v6 changelog entries.

## STAGE 9 — GUIDE CHARACTER ✅ COMPLETE

Owner-initiated, out of the original roadmap sequence — un-defers PRD Amendment 3's "AI Workforce"/visible-persona item, which was deferred "pending evidence of need" that still doesn't formally exist. Owner's explicit, deliberate call as solo project owner; goal stated as wanting "a successful and remarkable app."

**Concept:** a friendly guide character, Earth's-globe-for-a-head (in the spirit of Telegram's duck), named **Orbi** (provisional — trivially renamed via one prop default). Gentle, calm motion (float + a one-shot wave), not bouncy — deliberately reconciled with `theme.css`'s existing "purposeful, calm motion only" principle rather than silently overriding it.

- [x] **9.1** Scoped and named the character — Orbi, globe-headed, SVG + CSS (no animation library dependency), reacts to real page state rather than generic idle-only animation.
- [x] **9.2** Technical approach decided: plain SVG + CSS keyframes (`wv-guide-float`, `wv-guide-wave` in `theme.css`), consistent with this codebase's existing no-Tailwind/inline-style/CSS-custom-property pattern. No new dependency added.
- [x] **9.3** Built `GuideCharacter` in `packages/ui-components`, first wired into the sign-in page — mood reacts to the real auth form state (idle/thinking/happy/concerned), not a separate fake state invented for the character.
- [x] **9.4** Built the first-use tutorial (`GuideTutorial` component, generic shell in `packages/ui-components`; actual 3-step copy lives in `apps/web`'s dashboard page, per the packages-vs-apps split). Triggers once via `localStorage`, real persistence, not a mock.
- [x] **9.5** Expanded to every page: added to `AppShell` itself (not per-page), so Orbi follows the user across the dashboard and every current/future workspace for free.

**Fully verified:** 80/80 tests passing at Stage 9 completion, clean lint, clean format, clean production `next build`.

## STAGE 10 — WEATHER & CLIMATE (second workspace) ✅ COMPLETE

Also owner-initiated ahead of the 8.3 retrospective. Un-defers "any additional workspace beyond Agriculture" from this file's own deferred list, at the owner's explicit direction. **Weather & Climate** chosen specifically over the other 10 PRD-defined workspaces (Construction, Insurance, Renewable Energy, Logistics & Shipping, Disaster Monitoring, Education, Research, Government & NGOs, Public Explorer) because the PRD's own Section A.7 marks Disaster Monitoring as carrying "the platform's highest ethical weight... zero tolerance" (life-safety alerts) — too high-stakes for a second workspace on a solo project — while Weather & Climate (Section A.6) is the PRD's lowest-stakes, most general-purpose workspace, and could reuse `NasaPowerConnector` completely unchanged (different `parameters` config only: `T2M` instead of `GWETROOT`).

- [x] **10.1** `WeatherStatusProvider` (`services/interpretation-engine`) — temperature-band classification, same threshold-based/non-ML/auditable pattern as `SoilMoistureStatusProvider`. Fully tested (8 new tests), same confidence-from-data-sufficiency logic.
- [x] **10.2** Real validation that `NasaPowerConnector` needed zero code changes for a second, different parameter — the concrete evidence (not just design intent) that ADR-0002/0003's ingestion/interpretation boundary actually holds. Documented in `docs/data-provenance/nasa-power.md`.
- [x] **10.3** `apps/web/app/workspaces/weather/` — `workspace-shell.tsx` and `page.tsx`, mirroring Agriculture's structure exactly (same widget-grid pattern, same AI-panel wiring).
- [x] **10.4** Weather & Climate card added to the Home Dashboard.
- [x] **10.5** Map view for Weather & Climate — `apps/web/app/workspaces/weather/map/`, mirroring Agriculture's map page/MapView structure exactly, temperature-colored marker instead of moisture-colored. Verified: builds correctly at the same ~315kB size as Agriculture's map (both load MapLibre), correctly isolated to just these two routes.
- [x] **10.6** Forecast / short-medium-long-range confidence gradient — **real**, via a genuinely new second data-provider connector, `OpenMeteoConnector` (`services/data-ingestion`), closing the deferred-list "second data-provider connector" item at the same time (unlike 10.2's same-provider-different-parameter validation, this is a structurally different provider — see `docs/data-provenance/open-meteo.md`). Required an additive, fully-backward-compatible schema change (`NormalizedDataRecord.recordType`/`forecastIssuedAt`) — no existing connector, provider, or test needed to change. New `WeatherForecastProvider` produces a real lead-time-based confidence gradient (confidence genuinely decreases with forecast distance — short-range ≤3 days = high, medium ≤7 days = moderate, long >7 days = low), not a stylistic one. Wired into the Weather workspace home page's Trend widget, replacing the honest empty state. **Real licensing caveat found and documented, not glossed over**: Open-Meteo's free tier is non-commercial-use only — fine for this pre-launch project, but flagged in three places (the connector's doc comment, every record's `provenance.knownLimitations`, and `docs/data-provenance/open-meteo.md`) as something to revisit before any commercial launch.

**Fully verified:** 114/114 tests passing, clean lint, clean format, clean production `next build` — all four new/changed routes (`/workspaces/weather`, `/workspaces/weather/map`) build correctly and stay properly isolated (no bundle leakage into unrelated routes, checked directly against the build output after the earlier Orbi-3D bundle-leak lesson).

## STAGE 11 — AUTH EXPANSION: PASSWORD, GOOGLE OAUTH, REMEMBER ME, PAGE REDESIGN

Owner-initiated, another deliberate deviation from the 8.3-first sequencing rule. Expands sign-in beyond magic-link-only — a real product/security decision, not a small UI addition, since this app was originally built passwordless on purpose (see `docs/security/auth-threat-model.md`, threats #6–#8 for the honest accounting of what this adds).

- [x] **11.1** Password auth (`AuthService.signUpWithPassword`/`signInWithPassword`) — fits the existing service-role-key architecture unchanged, same pattern as magic link. 8-character floor plus real, pattern-aware strength feedback (not naive complexity rules).
- [x] **11.2** `PasswordStrengthMeter` (`packages/ui-components`), built on `@zxcvbn-ts` (the actively-maintained fork; the original `zxcvbn` package is unmaintained) — researched against current NIST SP 800-63B guidance (length over composition rules) before building, not assumed. Real tests confirm it scores a long passphrase higher than a short "complex-looking" password, and penalizes reusing the user's own email.
- [x] **11.3** Google OAuth — required introducing `@supabase/ssr` (a real, deliberate architecture addition, confirmed with the owner first rather than rushed) because PKCE's `code_verifier` needs to persist across requests, which the existing stateless service-role client structurally cannot do — the exact failure class that already broke magic link once. Verified against real Next.js/Supabase docs before writing code, specifically to avoid a repeat of the CSP incident's "assumed, not verified" mistake.
- [x] **11.4** Real "Remember Me" — required adding actual refresh-token handling (`AuthService.refreshSession`, a new `REFRESH_COOKIE`), since this codebase had none before; a cookie with a long Max-Age and nothing to refresh with would have been cosmetic, not functional.
- [x] **11.5** Login page redesign — split-screen layout, `AuthIllustration` (original SVG art extending the existing globe/Orbi motif — deliberately not a sourced photo; copyright/licensing reasoning is in the component's own doc comment), tabs between magic-link and password sign-in, Google button, Remember Me checkbox.
- [x] **11.6** `docs/security/auth-threat-model.md` updated with three new threats (#6 password credential-stuffing/brute-force, #7 the new OAuth/PKCE flow and the new `SUPABASE_ANON_KEY` credential, #8 the new persistent-access window Remember Me creates) — the file's own earlier "SSO/OAuth doesn't exist yet" line was real, dated content, now corrected rather than left stale.

**Honestly NOT done / verified:**

- ~~Breached-password screening~~ **Closed** — see "STAGE — BREACHED-PASSWORD SCREENING" below. ~~Rate limiting on password sign-in~~ **Closed** — see "STAGE — SIGN-IN RATE LIMITING" below. ~~Per-IP rate limiting~~ **Closed** — see "STAGE — PER-IP SIGN-IN LOCKOUT + PASSWORD-RESET RATE LIMITING" below. ~~Rate limiting on `requestPasswordReset`~~ **Closed** — same stage.
- **OAuth has not been exercised against a live Google Cloud OAuth app or live Supabase project** — it's correct against documented Supabase/Next.js behavior and builds/typechecks cleanly, but that is a different claim from "works in a real browser." **Real manual setup is required before this can work at all**: a Google Cloud OAuth Client ID/Secret, added to Supabase Dashboard → Authentication → Providers → Google, with `SUPABASE_AUTH_REDIRECT_URL`'s exact value registered as an authorized redirect URI in Google Cloud Console, plus the new `SUPABASE_ANON_KEY` env var set in Vercel. None of this is something a code change can do.
- **Only Google is wired up**, though the OAuth code path is provider-agnostic enough that adding a second provider (e.g. GitHub) later is a small, contained change, not a rewrite.

**Verified for real:** 94/94 tests passing, clean lint, clean format, clean production `next build` with every new env var set.

## STAGE 12 — CONSTRUCTION (3RD WORKSPACE)

Owner-initiated, un-defers "any workspace beyond Agriculture and Weather & Climate" from this file's own deferred list, at the owner's explicit direction. **Construction** chosen next of the remaining 9 PRD-defined workspaces (per PRD Section A.2) — same reasoning as Stage 10's choice of Weather & Climate: not the highest-stakes option (Disaster Monitoring stays deferred, per its own "zero tolerance" life-safety framing), and it could reuse `NasaPowerConnector` completely unchanged (a second parameter, `WS2M`, alongside the already-used `T2M` — no data-ingestion changes needed, the same validation Stage 10.2 already ran).

- [x] **12.1** `ConstructionRiskStatusProvider` (`services/interpretation-engine`) — cross-references current temperature (`T2M`) and wind speed (`WS2M`) against fixed, per-activity thresholds (concrete pour, crane operation, roofing work) and produces a go/caution/no-go recommendation with reasoning per activity, per PRD Section A.2's "How AI enhances the experience." Same threshold-based/non-ML/auditable pattern as `SoilMoistureStatusProvider` and `WeatherStatusProvider`, extended from one metric to two. Fully tested (12 new tests): normal conditions, cold/hot pour thresholds, high/elevated wind for crane and roofing, missing-metric graceful degradation, insufficient-data (never fabricated), confidence scaling, and `evaluate()` ground-truth matching.
- [x] **12.2** `apps/web/app/workspaces/construction/` — `workspace-shell.tsx` and `page.tsx` ("Today's Activity Status"), mirroring Agriculture's and Weather & Climate's structure exactly.
- [x] **12.3** Construction card added to the Home Dashboard.
- [x] **12.4** Map view — `apps/web/app/workspaces/construction/map/`, mirroring the existing map page/MapView structure exactly; marker colored by worst-case activity status (go/caution/no-go) rather than a single metric band. Verified: builds correctly at the same ~315kB size as the other two map routes, correctly isolated to just this route.

**Honest scope, stated plainly (not glossed over):**

- **No excavation/flash-flood activity, no lightning-proximity alert.** Both are named in the PRD. Excavation/flash-flood needs precipitation data (`PRECTOTCORR`) not currently ingested by any provider in this codebase. Lightning proximity needs a fundamentally different data source (strike detection) with no connector at all. Neither is faked under a name implying it works.
- **Thresholds are fixed defaults, not yet configurable per project/activity** — the PRD explicitly calls for "configurable per activity type" as the real target feature. The values used (5°C/32°C for pour, 8/13 m/s for crane, 8/12 m/s for roofing) are this implementation's own reasonable interpretation of commonly-cited general construction-safety guidance, not a transcription of any single binding standard, manufacturer load chart, or jurisdiction's code — stated in the provider's own doc comment, not presented as authoritative.
- **No terrain/flood-risk map overlay** — the PRD's map vision includes one; only the site-risk-status marker is built, same "base layers + one data overlay" narrow scope as the other two workspaces' maps.

**Not verified against the live NASA POWER API or Open-Meteo API from this build environment** — same caveat as every other workspace's page: this sandbox cannot reach power.larc.nasa.gov or api.open-meteo.com, so this is written to handle both outcomes but hasn't been exercised against a real request yet.

### STAGE 12 FOLLOW-UP — Site Risk Timeline + cross-workspace sidebar

Owner-initiated follow-up, same session. Two items:

- [x] **12.5** `ConstructionSiteRiskTimelineProvider` (`services/interpretation-engine`) — the forward-looking, multi-day Site Risk Timeline flagged as a gap in 12.1, now built. Consumes `OpenMeteoConnector`'s forecast records for both `T2M` and `WS2M`. `OpenMeteoConnector` itself was extended (additively — existing temperature-only callers and their tests are untouched) to also fetch `wind_speed_10m_max`, explicitly requested in m/s (`wind_speed_unit=ms`) so it's directly comparable to NASA POWER's `WS2M`. The timeline provider reuses `ConstructionRiskStatusProvider`'s exact exported per-activity threshold functions rather than a second copy, so the two can never silently disagree about what counts as risky. Lead-time-based confidence gradient, same reasoning as `WeatherForecastProvider`. Wired into the Construction home page, replacing the honest empty state from 12.2. 11 new tests (2 for the connector's wind path, 9 for the timeline provider): all-go, flagged risk dates, confidence scaling with lead time, partial-data days, ignoring non-forecast records, and insufficient-data (never a fabricated timeline).
- [x] **12.6** Cross-workspace sidebar switcher — "add a dashboard to the left side of the app so I can toggle between workspaces." `apps/web/app/workspaces/workspace-nav.ts` is a new small shared file (`WORKSPACE_LINKS`) listing all three workspaces; every workspace's `workspace-shell.tsx` and the Home Dashboard's sidebar now render it above their own page-specific items, so any workspace is reachable from any other without returning to `/dashboard` first. Extracted into a shared file rather than copy-pasted a third time — three shells needing the identical list is the concrete "genuine third consumer" signal Engineering Blueprint 4.5 calls for. No change to the `Sidebar`/`AppShell` components themselves — this is app-specific content (workspace names/routes), not a new UI primitive; the existing flat-list sidebar API was already sufficient.

**Fully verified:** 137/137 tests passing (11 new), clean lint, clean format, clean production `next build` — all three workspaces' routes build correctly at consistent sizes and stay properly isolated (checked directly against the build output).

---

## STAGE 13 — RENEWABLE ENERGY (4TH WORKSPACE, WIND ONLY)

Owner-initiated, same "continue with another workspace" direction as Stage 12. **Renewable Energy** chosen next of the remaining 8 PRD-defined workspaces (per PRD Section A.4) for the same reason Weather & Climate and Construction were chosen before it: reuses already-validated data pipelines completely unchanged. Unlike Stage 12, this workspace's primary widget (the PRD's own named **Asset Generation Outlook**) is forecast-based _from the start_ — both `NasaPowerConnector`'s `WS2M` (current) and `OpenMeteoConnector`'s wind forecast (added in the Stage 12 follow-up) were already proven, so there was no reason to hold the forecast widget back to a later ticket this time, unlike Construction's first cut.

**Insurance was considered and explicitly passed over for now** — the PRD's own cross-workspace notes (Section D, item 6) flag Insurance as needing "near-audit-grade rigor" for parametric triggers, plus a genuinely larger scope (multi-hazard synthesis across flood/wildfire/storm/drought, portfolio upload, audit-logged collaboration, regulatory-grade exports) that doesn't fit this ticket's size. A real scope decision, not a silent skip — noted here so it isn't lost.

- [x] **13.1** `WindGenerationStatusProvider` (`services/interpretation-engine`) — classifies current wind speed (`WS2M`, via `NasaPowerConnector` unchanged) into a generic turbine operating band (below-cut-in / ramping / rated-output / cut-out), per PRD Section A.4's "AI translates raw ... wind-speed ... data into asset-specific generation forecasts." Same threshold-based/non-ML/auditable pattern as every other status provider. 12 new tests.
- [x] **13.2** `WindGenerationOutlookProvider` — the PRD-named **Asset Generation Outlook**, forecast-based from day one. Consumes `OpenMeteoConnector`'s wind forecast records, reuses `WindGenerationStatusProvider`'s exact exported `classifyWindSpeed` function so the two can never silently disagree. Lead-time-based confidence gradient, same reasoning as `WeatherForecastProvider` and `ConstructionSiteRiskTimelineProvider`. 9 new tests.
- [x] **13.3** `apps/web/app/workspaces/renewable-energy/` — `workspace-shell.tsx`, `page.tsx` (Generation Outlook as the primary widget, Current Status secondary), and `map/` (marker colored by generation band), mirroring the other three workspaces' structure exactly.
- [x] **13.4** Renewable Energy card added to the Home Dashboard; `workspace-nav.ts`'s shared `WORKSPACE_LINKS` extended to include it, so the cross-workspace sidebar switcher (Stage 12.6) covers all four workspaces automatically.

**Honest scope, stated plainly:**

- ~~Wind only — no solar or hydro.~~ **Solar closed** — see "STAGE — RENEWABLE ENERGY FOLLOW-UP: SOLAR IRRADIANCE" below (irradiance level only, not generation output). Hydro still needs streamflow/hydrological data this codebase has no connector for at all.
- **No anomaly/underperformance detection.** The PRD's "How AI enhances the experience" calls for flagging underperformance relative to conditions (equipment issues vs. environmental causes) — that needs a real generation/output data feed from the asset itself, which doesn't exist anywhere in this codebase. This provider can only describe conditions, not compare them against real output.
- **Generic turbine operating envelope (3/12/25 m/s cut-in/rated/cut-out), not asset-specific.** Real turbines vary by model; this project has no per-asset power-curve data source, and the PRD's own user journey ("sign-up captures asset location(s) and type") implies per-asset configuration that isn't built. Stated in the provider's own doc comment, not presented as manufacturer-accurate.
- **No Portfolio Risk Map, no multi-asset siting/feasibility tools** — one demo asset location only, same limitation as every other workspace.

**Not verified against the live NASA POWER or Open-Meteo APIs from this build environment** — same caveat as every other workspace: this sandbox cannot reach either API directly.

**Fully verified:** 158/158 tests passing (21 new), clean lint, clean format, clean production `next build` — `/workspaces/renewable-energy` and its map route both build correctly and stay properly isolated (checked directly against the build output).

### STAGE 13 FOLLOW-UP — App shell rework: shared brand, sectioned sidebar, collapse

Owner-reported, same session: "the dashboard collides with itself when a workspace is chosen," plus two explicit asks — a real app logo/name placeholder in the header that returns to `/dashboard` on click, and a Ctrl+B-toggleable, collapsible left sidebar "same mechanism Claude has." All three are one underlying change: `Sidebar` and `AppShell` (`packages/ui-components`) were reworked, not worked around per-page.

- [x] **13.5** `Sidebar` now takes grouped `sections` (a `SidebarSection[]`, each with an optional heading) instead of one flat `items` list. Every workspace shell now renders two sections — **"Workspaces"** (the cross-workspace switcher) and **"This Workspace"** (that workspace's own pages, e.g. Site Risk/Map) — visually separated with group headings instead of interleaved in one list. This is the actual fix for the reported "collision": the switcher and the current workspace's own pages read as the same kind of thing before, sitting in one list with no separation. The Dashboard's own sidebar has only the "Workspaces" section, since there's no "this workspace" to show there.
- [x] **13.6** `AppBrand` (`apps/web/app/app-brand.tsx`) — a shared placeholder app logo ("WV" badge — a real logo asset doesn't exist yet, stated plainly, not dressed up) + "World Vitality" name, wrapped in a link back to `/dashboard`. Replaces what each workspace shell used to pass as its own `brand` (that workspace's own name, not clickable) — page-specific identity now lives in each page's own `pageTitle` heading instead, since the header brand slot is app-level chrome and should be identical on every page.
- [x] **13.7** Sidebar collapse, owned entirely by `AppShell` rather than threaded through every page: Ctrl+B or Cmd+B toggles it (`preventDefault`'d), and the sidebar's own rail has a click toggle too, since a keyboard-only control isn't discoverable to a first-time user. Persisted to `localStorage` — necessary because this app has no shared persistent layout wrapping every page (each page mounts its own `AppShell` instance), so without persistence the collapsed state would silently reset on every navigation. Collapsed state shows an icon-only rail; items without an explicit icon (all of them, today) fall back to a first-letter badge rather than an empty row — a deliberate minimal stand-in, not a real icon system (this project has no icon library dependency, and adding one wasn't in scope for this ticket).
- [x] **13.8** `layout.test.tsx` rewritten for the new `sections`/collapse API — including new coverage for section headings rendering, the collapse toggle button firing, Ctrl+B toggling and persisting to `localStorage`, and reading a previously persisted collapsed state on mount.

**Fully verified:** 163/163 tests passing (5 new/changed), clean lint, clean format, clean production `next build` — all four workspace routes plus `/dashboard` build correctly at consistent sizes.

**Not addressed in this ticket — Google OAuth consent screen showing `<project-ref>.supabase.co`:** raised by the owner in the same message, but this is entirely external configuration (Google Cloud Console + Google Search Console, and possibly Supabase's paid custom-domain add-on), not something fixable from this codebase. No code change corresponds to this item. See the conversation for the concrete steps given to the owner directly.

---

## STAGE 13 FOLLOW-UP #3 — Forgot Password

Owner-initiated. Raised alongside a request to store user accounts "in Firebase" — clarified with the owner first rather than silently built: **this app has used Supabase for identity since Stage 3** (magic link) and Stage 11 (password, Google OAuth, Remember Me); introducing Firebase alongside it would mean two separate user databases, a real architectural problem, not a small addition. Built on Supabase instead, which already does everything asked for. Also clarified: the magic sign-in link and a forgot-password link are two different flows in every real app (Supabase, Firebase, Auth0 alike) even though both arrive by email — this ticket builds Forgot Password as its own flow, not a repurposing of the sign-in magic link. "Auto-login when the user returns" already existed (Stage 11's Remember Me) — no new work needed there.

**Researched Supabase's current recommended implementation before building** (per this project's own established discipline) rather than assuming from training data — specifically checked for a documented historical footgun (the GoTrue-js client SDK firing a `SIGNED_IN` event before `PASSWORD_RECOVERY` on password-recovery links). Confirmed that gotcha is specific to apps using the client-side SDK's `onAuthStateChange` listener; this app has never used that (everything runs through stateless Server Actions and Route Handlers, the same `token_hash`/`verifyOtp` pattern already established for magic link), so the bug doesn't apply here — verified, not assumed.

- [x] **13.9** `AuthService`/`SupabaseAuthService`: three new methods — `requestPasswordReset` (sends the reset email, same account-existence-shouldn't-leak discipline as `requestMagicLink`), `verifyPasswordResetCallback` (`verifyOtp` with `type: "recovery"`, same self-contained stateless-server pattern as magic link's `type: "email"`), and `updatePassword` (uses the **admin** API — `auth.admin.updateUserById` — since this service's client is service-role/`persistSession: false` and never holds an ordinary "signed in" user session to call the non-admin `updateUser` against).
- [x] **13.10** `/auth/callback` extended with a third branch: `type=recovery` verifies the token, sets session cookies with `rememberMe` **forced false** regardless of any query param (a recovery-derived session must never become a persistent 30-day login), and redirects to `/reset-password` — deliberately not `/dashboard`, unlike the other two branches.
- [x] **13.11** Two new Server Actions in `lib/actions.ts`: `requestPasswordResetAction` and `updatePasswordAction`. The latter identifies _whose_ password to change from the session cookie itself (never a client-submitted value, which would be spoofable), and — a deliberate security choice — signs the user **out** immediately after a successful change rather than leaving the recovery-derived session active, since a reset flow only proves inbox access, not necessarily physical device control.
- [x] **13.12** Two new pages: `/forgot-password` (request the email) and `/reset-password` (set the new password, reusing `PasswordStrengthMeter` from Stage 11) — plus a "Forgot password?" link on the login page's password sign-in form, and a "Your password has been updated" message on return from a successful reset.
- [x] **13.13** `docs/onboarding/repository-setup.md` — added the required Supabase Dashboard step (customizing the Reset Password email template to use `token_hash`/`type=recovery`), mirroring the existing Magic Link template step from Stage 6, since without it `/auth/callback` never sees `type=recovery` and the whole flow silently degrades to "sign in without ever reaching the new-password page." `docs/security/auth-threat-model.md` — new threat #9, naming plainly that a recovery link genuinely authenticates the user by design (not a bug), and the three real mitigations built here (short session lifetime, forced non-persistent, sign-out-after-reset) alongside the one honestly still-open gap (no reset-request rate limiting beyond Supabase's platform default, same class of gap already recorded for password sign-in).

**Fully verified:** 163/163 tests passing (no new unit-testable logic — like every other `SupabaseAuthService` method, this depends on a live Supabase project, consistent with existing precedent of not mocking it), clean lint, clean format, clean production `next build` — `/forgot-password` and `/reset-password` both build correctly.

**Not verified against a live Supabase project from this build environment** — same caveat as every other auth flow in this codebase (magic link, password, Google OAuth): correct against Supabase's documented behavior and typechecks/builds cleanly, but hasn't been exercised against a real reset email + real click-through from this sandbox. Treat the first real attempt as the actual test.

**A pre-existing gap surfaced while building this, not introduced by it:** no page in this app actually checks `getSession()`/requires authentication server-side yet — `/dashboard` and every workspace route render regardless of whether a valid session cookie is present. This ticket only needed to _read_ the session cookie (for `updatePasswordAction`, to identify who's resetting), not build a general page-gating mechanism — that remains real, open follow-up work, recorded here rather than silently left unnoticed.

---

## STAGE 14 — RESEARCH (5TH WORKSPACE, DATASET EXPLORER)

Owner-initiated ("proceed with another workspace"). **Research** chosen of the remaining 6 PRD-defined workspaces (per PRD Section A.9) for a different reason than Weather/Construction/Renewable Energy were: those were chosen for connector reuse, but Research's own PRD design — "minimally interpreted, maximally transparent," raw data with full provenance rather than an AI-summarized layer — is close to a perfect match for what this codebase's ingestion layer (`NormalizedDataRecord` + `Provenance`, both fully built since Stage 1/2) already provides untouched. No new connector work, no new `InterpretationProvider` at all.

**Education, Government & NGOs, and Public Explorer were considered and passed over:** Education is explicitly flagged in its own PRD section as needing "the most conservative child-safety, data-privacy, and moderation design on the entire platform... as a hard requirement, not a later addition" — not something to build casually alongside four other tickets in one session. Government & NGOs and Public Explorer are both large, multi-domain-synthesis workspaces (jurisdiction-boundary risk maps, a personalized "Your Planet Today" feed) — real scope decisions, not silent skips, recorded here.

- [x] **14.1** `fetchDatasetAction` (`apps/web/lib/research-actions.ts`) — a thin Server Action that calls `NasaPowerConnector` or `OpenMeteoConnector` (unmodified, the exact same connectors every other workspace uses) and returns their raw `NormalizedDataRecord`s straight through, full `provenance` intact.
- [x] **14.2** `apps/web/app/workspaces/research/` — `workspace-shell.tsx` (deliberately **no** `aiInterpretation` prop, unlike every other workspace shell — see its doc comment for why building an interpretation layer here would work against this workspace's whole design principle) and `page.tsx`, the **Dataset Explorer**: pick a dataset (NASA POWER current conditions or Open-Meteo forecast) and, for NASA POWER, which metrics; results render in the `Table` component (whose own doc comment already named "Section 13's Research... case" as its motivating use) with every record's source, license, and known limitations shown inline; **real CSV export**, generated client-side from whatever's currently loaded.
- [x] **14.3** `map/` — a map view that deliberately does **not** color-code by any risk band (the one workspace map in this app that doesn't), showing the raw numeric reading in the marker's popup instead, per the PRD's "Full raw-layer geospatial visualization, minimally interpreted."
- [x] **14.4** Research card added to the Home Dashboard; `workspace-nav.ts`'s shared `WORKSPACE_LINKS` extended, so the cross-workspace switcher covers all five workspaces automatically.

**Honest scope, stated plainly:**

- **CSV export only** — the PRD also names NetCDF-equivalent and GeoTIFF-equivalent export formats; neither connector in this codebase ingests gridded/raster data that would need them, so CSV is the natural and sufficient format for what's actually here, not a partial implementation of a bigger export system.
- **No cross-source search/filter** — the PRD describes searching "all available underlying data sources with full metadata" in one place; this page lets you pick one of the two connectors that exist and fetch its current output. No saved queries, no cross-source search index — neither exists anywhere in this codebase.
- **No saved analyses, citation-block generation, or research-group collaboration** — all named in the PRD, none built. This ticket is the Dataset Explorer specifically, not the full workspace.
- Same single demo location as every other workspace; no real "capture research affiliation/purpose" sign-up flow.

**Not verified against the live NASA POWER or Open-Meteo APIs from this build environment** — same caveat as every other workspace.

**Fully verified:** 163/163 tests passing (no new unit-testable logic — this workspace calls existing, already-tested connectors directly with no new interpretation logic to test), clean lint (one real accessibility fix made along the way — a radio-button label ESLint correctly flagged as not having verifiably accessible text through nested custom components; fixed with an explicit `aria-label`, not suppressed), clean format, clean production `next build` — all five workspaces' routes build correctly and stay properly isolated.

---

## STAGE 14 FOLLOW-UP — External review, evaluated: Knowledge Graph Phase 1, telemetry, Decision Log, Glossary

Owner shared an external AI review of the project (explicitly written without access to the actual repository — evaluated on that basis, not taken at face value). Eight recommendations, sorted honestly into three buckets rather than built uniformly: **already done** (data confidence — every `InterpretationResult` already carries a real, non-decorative `ConfidenceLevel`); **premature for the current scale** (an API registry for "dozens of providers" when there are two; feature flags, when nothing in this app is built-but-hidden; a numbered error catalog, when structured categorized logging already exists) — not built, and said so directly rather than building unneeded scaffolding to look thorough; and **worth doing now**, which is this ticket.

- [x] **14.5** `packages/knowledge-graph` (ADR-0004, Decision #014) — Phase 1 only, explicitly not the full cross-domain causal-reasoning vision the external review described (see that ADR for exactly why: no `InterpretationProvider` in this codebase consumes another provider's output yet, so modeling causal chains now would mean inventing relationships with no real code behind them). A small, hand-maintained graph of `metric → capability → workspace`, checked directly against the real provider wiring at the time it was written. Surfaces a genuinely real, previously-implicit fact: `WS2M` (wind speed) feeds three completely different domain interpretations (Construction, Renewable Energy, Research) — the same reading, three different meanings by context. Query functions (`getRelated`, `metricsFeedingWorkspace`, `workspacesUsingMetric`) plus 10 new tests, including one that checks every edge references a real node (catches a broken graph at test time, not silently). Wired into a new "Data Relationships" panel on the Research workspace's Dataset Explorer.
- [x] **14.6** Telemetry (`logTelemetry`, `apps/web/lib/logger.ts`) — a third category on the existing structured-logging pipeline (`logApplication`/`logSecurity`'s same `write()` function), not a new system or a third-party vendor. Real events, not decorative: `workspace_viewed` on all five workspace home pages and the Home Dashboard, `dataset_fetched` on Research's Dataset Explorer, and the full auth funnel (magic link requested/verified, password signup/signin, Google OAuth verified, password reset requested/completed). Research's and the Dashboard's page components were split into a thin async server wrapper (logs the event) + the existing client component (unchanged interactive logic) specifically to get genuine server-side page-view logging — both were previously fully client-side with nothing to attach a server log call to. **Caught during verification, not shipped silently:** the first version of both wrappers had no dynamic-rendering directive, and Next.js's own production build output showed the telemetry event firing exactly once, during static prerendering, not per real request — inspecting the actual build output (not just "it compiled") caught this before it shipped as telemetry that would have silently only ever logged once, ever. Fixed with `export const dynamic = "force-dynamic"` on both wrappers, same as every other dynamic page in this app; re-verified the fixed build produces zero build-time log lines. Explicitly documented what this does NOT give (no aggregation/dashboard — needs a real vendor + consent decision not made here; no client-only interaction events like sidebar-collapse clicks; no per-user identity on page-view events) rather than letting "telemetry" imply more than what's actually built.
- [x] **14.7** `docs/Decision-Log.md` — 15 real decisions from this project's actual history (Decision #001 through #015), not hypothetical examples, each with a genuine reason and real alternatives that were actually weighed at the time. Cross-references the four existing ADRs rather than duplicating them.
- [x] **14.8** `docs/Glossary.md` — 13 real terms as actually used in the codebase, checked against real type names and doc comments rather than written from a generic template. Keeps the "Workspace ≠ module/application/dashboard" contrast format the external review suggested, since it was a genuinely good format for exactly this project's own real confusion risk (five workspaces, one application, one differently-named Dashboard page).

**Fully verified:** 173/173 tests passing (10 new — the knowledge graph), clean lint, clean format, clean production `next build`.

---

## STAGE 14 FOLLOW-UP #2 — Docs upkeep: README, architecture overview, incident-response runbook

Owner-initiated ("what's next in the docs?"). Three real, concrete gaps found by actually reading the current `docs/` folder rather than proposing speculative additions — different in kind from the external review's premature suggestions (API registry, feature flags, error catalog), which stayed correctly un-built.

- [x] **14.9** `docs/README.md` — was factually wrong ("ADR-0001 through 0003" when ADR-0004 exists), and didn't mention `docs/Decision-Log.md`, `docs/Glossary.md`, `docs/reviews/`, or `docs/security/` at all despite all four existing and being in active use.
- [x] **14.10** `docs/architecture/system-overview.md` — the `architecture/` folder had been an empty promise ("to be added as the system grows") since Stage 0. Filled with a single-page current-state reference: the three-layer diagram (ADR-0002), the real metric → capability → workspace table (cross-referencing `packages/knowledge-graph` as the live source of truth, not duplicating it), connectors, identity, observability, and deployment topology — genuinely missing before this, since BUILD_PLAN.md is a chronological changelog someone would have to read start to finish to reconstruct the same picture.
- [x] **14.11** `docs/runbooks/incident-response.md` — updated to reflect the app's real current surface area: SEV-2 examples broadened beyond Agriculture-only scenarios, the logger's third (`telemetry`) category added, the Sign-in Broken playbook split into magic-link/Forgot-Password/Google-OAuth-specific sections (previously only covered magic link — a real gap, since this exact conversation's Forgot Password email-template issue would have had no runbook entry), the data-provider-failure section extended to cover Open-Meteo (and its non-commercial-license boundary) alongside NASA POWER, and a new triage step for the exact static-vs-dynamic-rendering bug class caught during the telemetry work.

**Fully verified:** 173/173 tests passing (docs-only change, no new tests expected or added), clean lint, clean format, clean production `next build`.

---

## STAGE 14 FOLLOW-UP #3 — Real logo

Owner provided a real logo asset (a full lockup: globe/leaf/ribbon "W" mark + "WORLD VITALITY" wordmark + "SEE · UNDERSTAND · ACT" tagline) and asked for it to replace every placeholder in the app, specifically naming the auth pages' illustration panel ("the green square").

- [x] **14.12** Two crops produced from the source asset (`apps/web/public/brand/`): `world-vitality-mark.png` (icon only — globe/leaf/ribbon, no wordmark, for contexts that already render the "World Vitality" name as separate text) and `world-vitality-logo.png` (the full lockup, for hero/standalone contexts). A square `apps/web/app/icon.png` (Next.js's auto-detected favicon convention) generated from the mark, centered and padded onto a transparent square canvas.
- [x] **14.13** `AppBrand` (`apps/web/app/app-brand.tsx`) — the placeholder "WV" circular badge replaced with the real mark via `next/image` (correct here specifically because this file lives inside `apps/web` itself, not a shared package — see 14.14's note on why `AuthIllustration` deliberately does NOT do the same).
- [x] **14.14** `AuthIllustration` (`packages/ui-components`) — the "green square": an abstract original SVG scene (hills/sun/data-points on a green gradient), built at Stage 11 specifically because no real logo existed yet, now replaced with the real full-lockup logo on a plain white/neutral background (the real logo already carries its own full color story — blue/green/orange — so a green-tinted backdrop would compete with it rather than complement it, a deliberate change, not an oversight). Uses a plain `<img>`, not `next/image`, deliberately: this component lives in a shared, framework-adjacent package, not inside the Next.js app itself, and coupling a shared package's source to a specific consuming framework's Image component is the wrong direction of dependency even though it would likely still work in practice. Accessibility also changed correctly, not just cosmetically: the old SVG was rightly `aria-hidden` (purely decorative); the real logo carries real meaning (the tagline is part of the asset), so it now gets real `alt` text instead. Tests rewritten to match (2 tests, replacing 2 that asserted the old `aria-hidden` SVG behavior).
- [x] **14.15** `apps/web/app/layout.tsx` — real `metadata.icons`/`openGraph.images` pointing at the new assets, plus a `metadataBase` fix caught by the production build's own warning output (without it, Next.js can't correctly resolve the Open Graph image's absolute URL) — same "read the actual build output, don't just check it compiled" discipline that caught the telemetry static-rendering bug in the previous ticket.

**Honestly noted, not silently glossed over:** the source asset is a detailed, photographic-style illustration, not a simplified flat icon — at true favicon sizes (16×16/32×32 in a browser tab) some fine detail will be lost to compression/scaling. Building a simplified flat-icon variant would mean inventing a new design the owner didn't provide, not just resizing the one they did — left as-is rather than silently altering the provided mark's actual design.

**Fully verified:** 173/173 tests passing (2 changed to match the new implementation, 0 net new), clean lint, clean format, clean production `next build` (also caught and fixed one real warning from the build's own output — the missing `metadataBase`).

---

## STAGE 14 FOLLOW-UP #4 — Real logo, second pass (landing page + login form column)

Owner sent a screenshot of the sign-in page still showing the old placeholder. Follow-up #3 replaced `AppBrand` and `AuthIllustration`, but missed two spots.

- [x] **14.16** `apps/web/app/page.tsx` (the landing page) still had the original hardcoded 28×28 `var(--wv-accent)` rounded-square placeholder from before Stage 14 follow-up #3 existed — replaced with the real `world-vitality-mark.png` via `next/image`, same pattern `AppBrand` already uses.
- [x] **14.17** `/login`'s form column was text-only ("World Vitality," no icon) — the split-screen `AuthIllustration` panel with the full logo only renders at ≥768px, so on narrower viewports the form column is the only branding a visitor sees. Added the real mark icon next to the caption text (empty `alt`, since the adjacent caption text already announces "World Vitality" to screen readers).

**Fully verified:** 173/173 tests passing, clean lint, clean format, clean production `next build` (9/9 static pages, no new warnings). Pushed as commit `110038a`, Vercel deployment `dpl_cor8YvusFsphosYmtwE7aRoXjBDM` confirmed `READY`.

---

## STAGE — SERVER-SIDE PAGE GATING

Owner-initiated ("go ahead"), closing the gap first surfaced during Stage 13 follow-up #3 (Forgot Password) and carried in the deferred list since: **no page in the app actually required a valid session server-side** — `/dashboard` and every workspace route rendered for anyone who requested the URL, signed in or not.

- [x] **G.1** `apps/web/lib/require-session.ts` — a shared `requireSession()` helper: reads the `wv_session` cookie, validates it via the same `getAuthService().getSession()` call `updatePasswordAction` already uses in production, and redirects to `/login` if it's missing or invalid.
- [x] **G.2** `apps/web/app/dashboard/layout.tsx` — gates `/dashboard`.
- [x] **G.3** `apps/web/app/workspaces/layout.tsx` — gates all five workspace home pages and their `/map` sub-routes in one place, since a `layout.tsx` applies to its whole nested route tree.

**Deliberately a `layout.tsx` (Server Component) check, not Next.js Middleware:** Middleware runs on the Edge runtime by default, and this app has already been burned once by an untested framework-internals assumption becoming a real production crash (the 3D Orbi/`react-reconciler` incident). Reusing the exact, already-proven `getSession()` code path is the better-understood choice for this app specifically.

**Honest scope, stated plainly, not silently glossed over:** this checks the access-token cookie only. It does **not** attempt `refreshSession()` using the `wv_refresh` cookie when the access token has expired — Server Components can't set cookies during rendering (only Server Actions/Route Handlers can), so there'd be nowhere valid to write a refreshed cookie from inside a `layout.tsx`. A signed-in user with "Remember Me" checked whose access token has expired will currently be redirected to `/login` the same as anyone else, rather than being silently refreshed. This is a real, separate gap — recorded below, not fixed here — since fixing it properly needs Middleware or a Route Handler (where a response's cookies actually can be rewritten), not a bigger version of this same ticket.

**Fully verified:** 173/173 tests passing (no new unit-testable logic — same precedent as every other `SupabaseAuthService`-dependent code path in this app: depends on a live Supabase project, not mocked), clean lint, clean format, clean production `next build` (all 9 routes build correctly; `/dashboard` and every `/workspaces/*` route remain server-rendered on demand, now for an additional real reason).

**Not verified against a live Supabase project from this build environment** — same caveat as every other auth-dependent code path in this app (magic link, password, Google OAuth, Forgot Password): correct against the same `getSession()` contract those already use in production, typechecks and builds cleanly, but hasn't been exercised against a real signed-in/signed-out browser session from this sandbox. Treat the first real attempt (visiting `/dashboard` while signed out, and again while signed in) as the actual test.

---

## STAGE — SILENT SESSION REFRESH (Middleware)

Owner said "continue" — the direct next step from the page-gating ticket above, closing the one gap it deliberately left open.

- [x] **R.1** `apps/web/middleware.ts` — this app's first-ever Middleware file. Opportunistically refreshes an expired access-token cookie using the `wv_refresh` cookie when "Remember Me" was chosen, so a returning user isn't redirected to `/login` just because their short-lived access token expired between visits.

**Deliberately narrow, with a safe fallback baked in, not a duplicate authorization check:** Middleware's only job here is the _opportunistic_ refresh attempt. `requireSession()` in each layout still makes the actual sign-in-required-or-not decision, completely unchanged from the ticket above. If the refresh fails for any reason — revoked refresh token, a genuine Supabase outage, an Edge-runtime incompatibility this review didn't catch — the request falls through exactly as it did before this file existed: `requireSession()` finds no valid session and redirects to `/login`. A bug here degrades to "asked to sign in again," never to a broken or insecure page.

**Verified before writing, not assumed, given this app's first use of Middleware (which runs on the Edge runtime by default) and given the CSP incident this project has already lived through once from an untested framework-internals assumption:** read `SupabaseAuthService.ts` directly and confirmed its `refreshSession()` call path is plain `@supabase/supabase-js` `fetch()` calls with no Node-only API anywhere — Edge-compatible by inspection, not by hope. Read Next 15.5.22's actual shipped `.d.ts` files (not recalled from training) to confirm `NextResponse.next({ request })`, `RequestCookies.set(name, value)`, and `ResponseCookies.set(name, value, options)` are the real, current signatures before using them. This is also the documented pattern Supabase's own official Next.js SSR guide recommends for exactly this purpose — not an invented approach.

**Fully verified:** production `next build` succeeded with the Middleware bundle compiling cleanly for the Edge runtime (`ƒ Middleware 89.1 kB`, no errors) — confirms Edge-runtime compatibility for real, not just by code inspection. 173/173 tests passing, clean lint, clean format.

**Not verified against a live Supabase project or a real browser from this build environment** — same caveat as every other auth code path in this app. Correct against the documented Middleware/cookie APIs and compiles cleanly for the Edge runtime, but the actual "does a Remember Me session survive an expired access token" behavior hasn't been exercised against a real signed-in browser session from this sandbox. Treat the first real test as: sign in with "Remember Me" checked, wait for (or manually expire) the access-token cookie, then visit `/dashboard` again and confirm no redirect to `/login` occurs.

---

## STAGE — BREACHED-PASSWORD SCREENING

Owner-initiated ("move on to the next stage"), closing the gap flagged in this file's own Known Gaps section since Stage 13 follow-up #3: password sign-up and reset only enforced a length floor, never checked whether the chosen password had already appeared in a known data breach.

- [x] **B.1** `apps/web/lib/password-breach-check.ts` — `checkPasswordBreach()`, using Have I Been Pwned's documented k-anonymity range API. Only the first 5 hex characters of the password's SHA-1 hash are ever sent over the network — never the password, never the full hash — matching HIBP's own documented privacy design, not an invented shortcut.
- [x] **B.2** Wired into `signUpWithPasswordAction` and `updatePasswordAction` (the password-reset completion step) — both reject a breached password with a clear, specific message before it ever reaches Supabase.

**Fails open, deliberately and honestly:** a network/API failure against the third-party HIBP service returns `{ breached: false, skipped: true }` rather than blocking sign-up — logged via `logSecurity` so a real, sustained outage is still visible, but never punishing a legitimate user for an unrelated service being briefly unreachable. This is a defense-in-depth check, not the primary defense (that's the length floor plus Supabase's own account security) — same fail-open-with-logging tradeoff already used for Google OAuth's config-missing case.

**Real gap, honestly flagged, not silently worked around:** `checkSuffixAgainstRangeResponse` (the pure, hash-matching part of this) is deliberately written to be unit-testable — but `apps/web` has never actually been wired into this repo's root `tsc --build` project-reference graph (see `tsconfig.json`'s `references` list), so it has zero test files today, and a `.test.ts` placed there would silently never run under `pnpm run test`. Rather than either skip verification entirely or add a misleadingly-labeled test file that never executes, the pure function was verified manually against a realistic HIBP-shaped fixture (confirmed against Node's own `crypto.createHash("sha1")` output directly) before shipping. Wiring `apps/web` into the test runner is a real, separate, **pre-existing** gap this ticket did not introduce — worth its own ticket, not something to bolt on as a side effect of one feature.

**Rate limiting, the other half of this stage's original name, deliberately NOT built this pass:** real rate limiting needs state that survives across serverless function invocations (in-memory counters don't persist between Vercel Lambda cold starts/regions) — meaning it needs a persistent store. The only already-trusted external service this app has is Supabase itself, and adding a new table/migration to the **live production** Supabase project is a consequential, hard-to-reverse infrastructure change this sandbox has no way to test against safely, and wasn't something the owner explicitly asked for this pass. Recorded below as the next real, still-open piece of this same gap, not silently dropped.

**Fully verified:** production `next build` succeeded (all 19 routes, Middleware bundle unaffected). 173/173 existing tests passing (unchanged — no new wired tests, per the honestly-flagged gap above), clean lint, clean format.

**Not verified against a live Supabase project or the real pwnedpasswords.com API from this build environment** — same caveat as every other network-dependent code path in this app. The hash-and-match logic is verified correct against Node's own SHA-1 implementation and a realistic fixture; the actual live HTTP call to HIBP has not been exercised from this sandbox (no outbound access to api.pwnedpasswords.com from this network allowlist, same class of limitation already documented for `NasaPowerConnector`/`OpenMeteoConnector`).

---

## STAGE — MODERN LOADING SCREEN

Owner asked for a modern loading screen, referencing an Instagram-style shimmer skeleton generated by a framework ("Boneframer") that reads the app's own CSS. That specific framework couldn't be found to exist under that name or description — evaluated honestly rather than either building blind or dismissing the ask. Real goal delivered a different, safer way instead, confirmed with the owner first.

- [x] **L.1** `packages/ui-components/src/layout/AppShellSkeleton.tsx` — a full-page, `AppShell`-shaped shimmer skeleton (sidebar-width bar + header + card grid), built entirely from `Skeleton` (already existed, Stage 5.2, but was unused anywhere in the app until now) and the same `--wv-*` design tokens every other component draws from — automatically theme- and dark-mode-correct because it shares the source, not a copy of it.
- [x] **L.2** `apps/web/app/dashboard/loading.tsx` and `apps/web/app/workspaces/loading.tsx` — Next.js App Router's own built-in loading-UI convention, one file each, covering all six top-level authenticated pages (Dashboard + 5 workspaces + their `/map` sub-routes) automatically.

**Why this, not a new dependency:** searched again for "Boneframer" by name and by the behavior described (auto-generating skeletons from an app's real CSS) — no such library or framework was found to exist. Building on an unverified library sight-unseen is exactly the mistake behind this app's one real production incident (the 3D Orbi/`react-reconciler` crash) — proposed the design-tokens-based alternative instead and got explicit confirmation before building it, rather than guessing at an integration for something unconfirmed to exist.

**Fully verified:** production `next build` succeeded, all 19 routes unaffected (bundle sizes essentially unchanged — `Skeleton` was already bundled; `AppShellSkeleton` adds negligible size). 173/173 tests passing, clean lint, clean format.

---

## STAGE — SIGN-IN RATE LIMITING

Owner-initiated, explicit go-ahead given for the live-production-infrastructure step this required (a new table + RPC functions on the live Supabase project). Closes the last piece of the gap flagged since Stage 13 follow-up #3 and named directly in `docs/security/auth-threat-model.md` Section 6: "no account lockout or progressive delay after repeated failures."

- [x] **R.1** Confirmed the connected Supabase project (`Supabase:list_projects`) before touching anything — exactly one project, named `world-vitality`, matching the live app. Inspected existing schema/RLS conventions (`profiles`/`workspace_members`/`data_export_requests` all have RLS enabled with zero broad policies) before designing the new table, to match the project's own established pattern rather than inventing a new one.
- [x] **R.2** Applied via `Supabase:apply_migration` (named `auth_rate_limits`, tracked): a `public.auth_rate_limits` table (email primary key, RLS enabled with zero policies — service-role access only, same trust boundary every other identity table already uses) plus three `security definer` RPC functions: `is_signin_locked` (read-only check), `record_failed_signin_attempt` (sliding-window counter: 5 failures within 15 minutes locks the account for 15 minutes), `record_successful_signin` (clears the record).
- [x] **R.3** Verified the migration against real inputs directly in SQL _before_ writing any application code that depends on it: confirmed a never-seen email reports unlocked, 5 simulated failures trigger a real lockout with a real `locked_until` timestamp, a successful sign-in clears the row back to zero. Test rows deleted afterward — no leftover fixture data in the live table.
- [x] **R.4** `AuthService`/`SupabaseAuthService`: added `checkSignInLockout`, `recordFailedSignIn`, `recordSuccessfulSignIn`, calling the three RPC functions above. Kept as separate methods from `signInWithPassword` rather than baked into it, so that method's contract stays "does this password match," not entangled with a lockout policy a caller might reasonably want to apply differently.
- [x] **R.5** `signInWithPasswordAction` checks lockout _before_ ever calling Supabase's own sign-in API — a known-locked-out account doesn't spend that budget on a request already known to fail — and records failure/success around the actual sign-in attempt. A locked-out user sees a specific "too many failed attempts" message rather than the generic invalid-credentials one, since the account is already protected from further guessing regardless of what the message says, and the more specific message is genuinely more useful to a legitimate locked-out user.

**Why per-email, not per-IP, stated honestly as a real scope choice, not an oversight:** this protects a specific targeted account from being brute-forced, which is the more direct and common shape of credential-stuffing at this app's current scale. An attacker rotating through many different target emails from one IP isn't slowed by this mechanism — recorded in the threat model doc as the next, still-open piece of this same gap, not silently unmentioned.

**Fully verified:** production `next build` succeeded (all 19 routes, Middleware bundle unaffected). 173/173 existing tests passing (no new unit-testable logic — the new code is entirely RPC calls to a live database, same precedent as every other Supabase-dependent auth code path in this app), clean lint, clean format. Unlike every other auth code path in this app, the actual database logic here **was** verified against the real, live Supabase project directly (not just the calling code compiling) — see R.3.

---

## STAGE — PUBLIC EXPLORER WORKSPACE (6th of 11 PRD workspaces, first of the six previously-unbuilt)

Owner said "proceed with the 6 unbuilt PRD [workspaces]." Six full workspaces is a multi-week effort, not a single pass — started with the one the PRD itself effectively recommends going first, rather than picking arbitrarily.

**Why Public Explorer, not one of the other five:** PRD Section D's Open Questions explicitly ranks stakes across workspaces — "Public Explorer, Education can tolerate more exploratory, lower-stakes AI interaction; others (Insurance, Disaster Monitoring, Government) require near-audit-grade rigor" — and separately recommends "explicit investment... for this workspace independent of its conversion-to-paid-workspace function" as a strategic, public-trust asset. It also needs no enterprise SSO or complex permission model (PRD A.11: "Anonymous... Registered... no paid tier"), unlike Insurance/Government & NGOs/Research/Education's institutional requirements. Insurance (financial/regulatory stakes), Disaster Monitoring (life-safety, zero-tolerance), Government & NGOs (institutional), and Education (explicit child-safety design requirements) all deserve dedicated, unhurried attention — not a first pass done quickly to check a box. Logistics & Shipping has no stakes disqualifier but also no PRD-stated urgency; left for a later pass.

- [x] **PE.1** `apps/web/lib/geocode.ts` — `geocodeLocation()`, resolving free-text place names to coordinates via Open-Meteo's free Geocoding API (same trusted provider family as `OpenMeteoConnector`, not a new dependency). This is the workspace's real differentiator: every other workspace shows one hardcoded demo location; this is the first genuinely user-driven "any place on Earth" search in the app.
- [x] **PE.2** `apps/web/lib/explore-actions.ts` — `exploreLocationAction()`, a Server Action reusing the exact same, already-proven `NasaPowerConnector` + `WeatherStatusProvider` pair the Weather & Climate workspace uses, driven by the geocoded location instead of a hardcoded one. Deliberately not a new interpretation capability — reusing a real, already-built one for an arbitrary location is a further real test of ADR-0002's ingestion/interpretation boundary, not a shortcut.
- [x] **PE.3** `apps/web/app/explore/` — `page.tsx` (thin server wrapper, telemetry only), `explore-view.tsx` (client search form + result rendering, reusing `Card`/`Text`/`Button`/`Input`/`ConfidenceBadge`/`StateDisplay` — zero new UI primitives), `explore-shell.tsx` (same `AppShell` wrapper pattern every `workspace-shell.tsx` already uses).
- [x] **PE.4** **Deliberately outside `app/workspaces/` and `app/dashboard/`, and therefore outside both route trees' `requireSession()` gates** — PRD A.11 is explicit: "No sign-up required for first exploration." This is the one page in the app intentionally reachable by a fully anonymous visitor; not an exception carved into the existing gates, a genuinely separate top-level route the gates never applied to.
- [x] **PE.5** Landing page (`app/page.tsx`) gained a second, equal-weight "Explore the planet" button next to "Sign in" — previously the only path from the landing page was straight to sign-in, which directly contradicted PRD A.11's "no sign-up required" mission by making the anonymous path undiscoverable. Caught while shipping this, not left as a silent gap.

**Honest scope, stated plainly, not silently glossed over — most of PRD A.11 is NOT built:** the "Your Planet Today" personalized story feed, the delight-oriented global explorable map, shareable "story card" social exports, opt-in alerts for saved locations, social sharing, and the open-ended AI assistant chat (the PRD's own example: "why is it raining so much this year where I live?") are all still PRD-only. This ships the one real, working piece — arbitrary free-text location search with a genuine environmental insight, fully anonymous — not a mocked-up preview of the rest. Registered-user features (saved locations, digests) also aren't built, since no user-configured-locations data model exists yet anywhere in this app (same pre-existing gap every other workspace's demo-location approach already documents).

**Fully verified:** production `next build` succeeded (`/explore` compiles and renders correctly, confirmed NOT covered by the Middleware matcher — `/dashboard/:path*` and `/workspaces/:path*` only, so no session-refresh attempt fires on an anonymous page either). 173/173 tests passing, clean lint, clean format.

**Not verified against the live NASA POWER or Open-Meteo Geocoding APIs from this build environment** — same caveat as every other network-dependent code path in this app (no outbound access to power.larc.nasa.gov or geocoding-api.open-meteo.com from this sandbox).

---

## STAGE — LOGISTICS & SHIPPING WORKSPACE (7th of 11 PRD workspaces, second of the six previously-unbuilt)

Continuing the same "one at a time, on the merits" approach as Public Explorer above, not picking arbitrarily.

**Why Logistics & Shipping next:** unlike Insurance (financial/regulatory near-audit-grade rigor), Disaster Monitoring (life-safety, zero-tolerance), Government & NGOs (institutional/SSO), and Education (explicit child-safety design requirements) — all of which deserve dedicated, unhurried attention before starting — Logistics & Shipping has no stakes disqualifier and needs no new architecture (standard authenticated `/workspaces/*` route, same session gate every other workspace already sits behind). It's also the one PRD Section A.5 workspace whose core mechanic (a wind-speed-based risk band) is nearly identical in shape to Construction's and Renewable Energy's already-proven pattern — the lowest-risk next pick, not the most PRD-urgent one.

- [x] **L1.1** `services/interpretation-engine/src/providers/logistics-route-risk-provider.ts` — `LogisticsRouteRiskProvider`, classifying `WS2M` wind speed (via `NasaPowerConnector`, unchanged) into a route-disruption risk band (clear/elevated/high/severe). Thresholds are this project's own reasonable interpretation of commonly-cited Beaufort-scale route-disruption caution bands, explicitly flagged as not any specific port authority's or regulatory body's official advisory — same honesty discipline as `ConstructionRiskStatusProvider`/`WindGenerationStatusProvider`. `classifyRouteRisk` exported for a future forecast-based outlook provider, same reuse pattern as `classifyWindSpeed`.
- [x] **L1.2** `apps/web/app/workspaces/logistics/` — `page.tsx` (Route Risk Status home), `map/page.tsx` + `map/MapView.tsx` (base layers + one data overlay, same pattern as every other workspace's map), `workspace-shell.tsx` (identical `AppShell` wrapper pattern). Added to `workspace-nav.ts` (cross-workspace sidebar) and `dashboard-view.tsx` (Home Dashboard tile).

**Honest scope, stated plainly, not silently glossed over — this is a small fraction of PRD A.5's actual ambition:** wind speed only, at a single hardcoded demo point — no multi-waypoint route data model exists anywhere in this app, no storm-track/port-status/flooding data source exists, and there's no forecast-based outlook yet (current conditions only, same incremental status-first pattern Construction and Renewable Energy both followed). Alerts, disruption reports, and fleet-wide summaries are honest empty states, same gap every other workspace's first cut already documents.

**Fully verified:** production `next build` succeeded (`/workspaces/logistics` and `/workspaces/logistics/map` both compile and render correctly, still covered by the existing session gate and Middleware — no new route-tree exception needed, unlike Public Explorer). 185/185 tests passing (12 new, covering `classifyRouteRisk`'s band boundaries and the provider's confidence/insufficient-data/ground-truth-evaluation behavior — same test shape as `WindGenerationStatusProvider`'s), clean lint, clean format.

**Not verified against the live NASA POWER API from this build environment** — same caveat as every other workspace's page.

---

## STAGE — PER-IP SIGN-IN LOCKOUT + PASSWORD-RESET RATE LIMITING

Owner-initiated ("proceed with the IP rate limiting and forgot-password"), closing the two remaining pieces flagged as open after "STAGE — SIGN-IN RATE LIMITING": per-IP sign-in protection, and rate limiting on `requestPasswordReset`.

- [x] **IP.1** Applied via `Supabase:apply_migration` (migration name `rate_limiting_ip_and_password_reset`, tracked) directly to the live Supabase project: `public.auth_signin_ip_rate_limits` table + `is_signin_ip_locked`/`record_failed_signin_attempt_ip` RPC functions — 20 failures within 15 minutes locks an IP for 15 minutes (deliberately a higher threshold than the per-email lockout, since one IP can represent many real users behind NAT/a shared network). Deliberately no "clear on success" function, unlike the per-email table — a successful sign-in from one account on a shared IP doesn't prove that IP is safe; the sliding window's natural decay is the only reset mechanism, on purpose.
- [x] **IP.2** Same migration: `public.auth_password_reset_requests` table + `record_password_reset_request` RPC function — a request-volume cooldown (not a lockout, since a reset request has no "wrong password" outcome to distinguish), 3 requests per email per 15 minutes.
- [x] **IP.3** Verified both against real inputs directly in SQL _before_ writing any application code: 19 simulated IP failures → not locked, 20th → locked with a real `locked_until`; 3 simulated reset requests → all allowed, 4th → blocked. Test rows deleted afterward.
- [x] **IP.4** `AuthService`/`SupabaseAuthService` gained `checkSignInIpLockout`, `recordFailedSignInIp`, `recordPasswordResetRequest`, calling the new RPC functions — same separate-methods pattern as the per-email lockout, kept uncoupled from `signInWithPassword`/`requestPasswordReset`'s own contracts.
- [x] **IP.5** `apps/web/lib/get-client-ip.ts` — best-effort client IP extraction via `headers()` (`x-forwarded-for`, Vercel's documented behavior for the real client behind its edge network; `x-real-ip` fallback). Returns `null` — not a fabricated placeholder — when neither header is present, and the caller skips IP-based limiting for that request rather than rate-limiting against a made-up key.
- [x] **IP.6** `signInWithPasswordAction` checks IP lockout (cheaper, coarser signal) before email lockout, before ever calling Supabase's own auth API. `requestPasswordResetAction` calls `recordPasswordResetRequest` before `requestPasswordReset` — once the cap is hit, no further reset emails go out until the window clears.

**Does not leak account existence, stated explicitly because it's worth checking, not assuming:** the password-reset rate limit is recorded and enforced identically regardless of whether the email belongs to a real account (the RPC call always runs first, unconditionally) — a spammed nonexistent address hits the exact same "too many requests" response a spammed real one does.

**Fully verified:** production `next build` succeeded (all 20 routes, Middleware bundle unaffected — `Middleware 89.3 kB`, unchanged from before this stage since the IP check happens inside the existing `signInWithPasswordAction` Server Action, not Middleware itself). 185/185 existing tests passing (no new unit-testable logic — entirely RPC calls to a live database, same precedent as the per-email lockout), clean lint, clean format. Unlike most auth code in this app, the actual database logic here **was** verified against the real, live Supabase project directly — see IP.3.

**Not verified against a live Vercel deployment/real browser from this build environment** — `x-forwarded-for` is Vercel's documented behavior for identifying the real client IP, not an assumption invented here, but hasn't been observed directly against a real incoming request from this sandbox.

---

## STAGE — DISASTER MONITORING WORKSPACE (8th workspace, third of the six previously-unbuilt)

Owner explicitly chose this one after being offered all four remaining PRD workspaces. Unlike Public Explorer and Logistics & Shipping, this one was **not** built to the same "narrow slice of the existing pattern" playbook without discussion first — Constitution Section A.7's own "Special note" flags this workspace as carrying "the platform's highest ethical weight," with "zero tolerance" for anything resembling false precision or engagement-optimization. A proposed scope was laid out and explicitly confirmed with the owner before any code was written.

**Why this shape, not a reused weather-threshold pattern:** every other workspace's AI-interpreted "risk status" (Construction, Renewable Energy, Logistics) is built from temperature/wind data this app already has — appropriate for those lower-stakes domains, but inventing a "disaster risk score" from the same data here would look like real hazard monitoring in exactly the moment someone's life-safety decision depends on it being real. Researched and found a genuinely better-fitting alternative instead: `api.weather.gov`, the U.S. National Weather Service's free, no-key-required, official public alerts API — real government data, not this app's own inference.

- [x] **D.1** `apps/web/lib/nws-alerts.ts` — `fetchActiveAlerts()`, fetching live official NWS alerts for a point. Deliberately **not** a `DataIngestionConnector`: that interface's contract requires normalizing into `NormalizedDataRecord` (a single numeric metric/value), and an alert is a structured event (headline, severity, description, effective/expires window), not a numeric reading — same reasoning already applied to `geocode.ts`/`password-breach-check.ts`.
- [x] **D.2** `apps/web/app/workspaces/disaster-monitoring/` — `page.tsx` (Active Alerts list, exactly as issued), `map/page.tsx` + `map/MapView.tsx` (marker colored by NWS's own `severity` field, not an invented risk band), `workspace-shell.tsx`.
- [x] **D.3** **Deliberately no `aiInterpretation` prop on the shell, no AI summarization or scoring of alert content anywhere** — same honest-empty-AI-panel precedent `app/workspaces/research/workspace-shell.tsx` established, applied here for a graver reason (Constitution's explicit zero-tolerance language for this specific workspace, not a transparency preference). Alerts are relayed as the government issued them, not reinterpreted.
- [x] **D.4** A permanent, visible disclaimer on the page: "This is not a substitute for official guidance — always follow instructions from local authorities."
- [x] **D.5** Added to `workspace-nav.ts` (cross-workspace sidebar) and `dashboard-view.tsx` (Home Dashboard tile), same as every prior workspace.

**Honest scope, stated plainly — this covers a small fraction of PRD A.7's actual ambition:**

- **U.S. and territories only** — NWS has no coverage elsewhere, a real, structural limitation of this data source, not an oversight. Every other workspace's shared demo location is deliberately outside the U.S. (Nigeria); this workspace uses its own U.S. demo point (Los Angeles, CA) for exactly this reason.
- ~~No fire-perimeter/flood-extent map layers, no evacuation-zone status, no shelter locations~~ — **Flood data, shelter locations, and fire detections all added** (see "STAGE — DISASTER MONITORING FOLLOW-UP" entries below). Live evacuation-zone status remains genuinely un-built — no unified free real-time source exists for it.
- **No multi-hazard AI cross-validation** (satellite fire detection, flood gauges) — this relays one government feed responsibly; it does not synthesize multiple hazard signals into anything.
- **No agency coordination features, no offline-first emergency mode, no SMS/push delivery** — all PRD-only.

**Fully verified:** production `next build` succeeded (`/workspaces/disaster-monitoring` and its map both compile and render correctly, still covered by the existing session gate — no new route-tree exception needed, unlike Public Explorer). 185/185 existing tests passing (no new unit-testable logic — this workspace deliberately has no interpretation provider to test, its entire design is relaying an external API's own structured response, not classifying it), clean lint, clean format.

**Not verified against the live api.weather.gov API from this build environment** — same caveat as every other network-dependent code path in this app (this sandbox cannot reach api.weather.gov).

---

## STAGE — DISASTER MONITORING FOLLOW-UP: FLOOD + SHELTER DATA

Owner asked to fill in the workspace's honestly-flagged gaps. Researched what's actually available as free, no-key, official U.S. data before building anything — same discipline as the workspace's original build. Found two genuinely real sources; declined a third (fire perimeters, needs a key the owner is separately obtaining) and a fourth (evacuation-zone status — no unified free real-time API exists) rather than fabricate either.

- [x] **DF.1** `apps/web/lib/usgs-flood-impacts.ts` — `fetchFloodingLocations()`, the USGS Real-Time Flood Impact (RT-FI) API (`api.waterdata.usgs.gov/rtfi-api`, free, no key required at this app's request volume). Real currently-flooding infrastructure locations, not an inferred flood score. Endpoint shape confirmed from a real, working third-party client's source code, since this sandbox has no outbound access to fetch USGS's own OpenAPI schema — parsing is deliberately defensive given that uncertainty, stated explicitly in the file rather than presented as verified.
- [x] **DF.2** `apps/web/lib/fema-shelters.ts` — `fetchNearbyShelters()`, FEMA/American Red Cross designated shelter facilities from HIFLD's open `national_shelter_system_facilities` layer, queried via the standard, long-documented Esri ArcGIS REST spatial-query pattern. `SHELTER_STATUS_CAVEAT` carries FEMA/HIFLD's own warning verbatim — "should not be used to determine the operational status of a facility during an active emergency" — displayed on-page unsoftened, not paraphrased into something that reads more reassuring than it is.
- [x] **DF.3** `page.tsx` gained two more sections (Flood Impact Locations, Nearby Designated Shelters), same honest empty/error states as Active Alerts, same "no AI interpretation" design — both are relayed exactly as their source returns them.
- [x] **DF.4** Provided the owner a step-by-step guide for obtaining a free NASA FIRMS `MAP_KEY` (fire perimeter/hotspot data) — not built into the app yet, since this app cannot self-register for a key requiring an email account it doesn't have. Wiring it in is explicit follow-up work once the owner has a key.

**Declined, not just unbuilt, stated explicitly why:** multi-hazard AI cross-validation/synthesis of the alert, flood, and shelter data into a single "here's the real risk" verdict was explicitly declined, even though it's technically buildable — doing so would directly contradict this workspace's own confirmed design (relay each official source as-issued, add no AI-generated interpretation layer) and reintroduce the exact false-precision risk that design exists to prevent. The three sections are shown side by side on one page — still zero synthesis, just more real sources in one place.

**Fully verified:** production `next build` succeeded (`/workspaces/disaster-monitoring` unaffected in route count, 118 kB First Load JS, in line with every other workspace page). 185/185 existing tests passing (unchanged — no new unit-testable logic, same precedent as the workspace's original build), clean lint, clean format.

**Not verified against any of the three live APIs (api.weather.gov, api.waterdata.usgs.gov, maps.nccs.nasa.gov) from this build environment** — same caveat as every other network-dependent code path in this app, stated with extra weight here given `usgs-flood-impacts.ts` and `fema-shelters.ts`'s additional field-name uncertainty (their exact response schemas could not be confirmed against a live request from this sandbox either).

---

## STAGE — DISASTER MONITORING FOLLOW-UP: FIRE DETECTIONS

Owner registered for and provided a free NASA FIRMS `MAP_KEY` following the step-by-step guide from the previous follow-up.

- [x] **DFI.1** `apps/web/lib/nasa-firms.ts` — `fetchActiveFireDetections()`, NASA FIRMS's area API (`VIIRS_SNPP_NRT` source, ~1° bounding box, last 24 hours). **The key itself is never hardcoded anywhere in this repository** — read only from `process.env.NASA_FIRMS_MAP_KEY`, which the owner must set in Vercel's environment variables (same place `SUPABASE_SERVICE_ROLE_KEY` and every other secret in this app already lives — this agent has no tool to set Vercel environment variables directly).
- [x] **DFI.2** Fails gracefully and distinctly on a missing key: `fetchActiveFireDetections` throws a specific, recognizable error the page checks for and renders as an honest "not configured" state — not the generic "couldn't reach the service" error, and not silently omitting the section. Confirmed the production build succeeds cleanly with the environment variable unset (all workspace pages are server-rendered on demand, not evaluated at build time, so a missing key at build time is a non-issue — only matters at request time, and is handled).
- [x] **DFI.3** Added as a fourth section on `page.tsx`, same honest empty/error/not-configured states as the other three, same zero-AI-interpretation design. `FIRE_DETECTION_CAVEAT` shown verbatim: these are satellite-detected thermal anomalies, **not confirmed wildfires** — agricultural burning and other heat sources can appear in the same feed.

**Fully verified:** production `next build` succeeded both with and without `NASA_FIRMS_MAP_KEY` set (all 20 routes, Middleware bundle unaffected). 185/185 existing tests passing (unchanged), clean lint, clean format.

**Not verified against the live firms.modaps.eosdis.nasa.gov API from this build environment** — same caveat as the workspace's other three data sources; this sandbox has no outbound access to that host either, so the CSV column-name assumptions (documented by FIRMS, not guessed) are unconfirmed against a real response.

---

## STAGE — GOVERNMENT & NGOS WORKSPACE (9th workspace, fourth of the six previously-unbuilt)

Owner explicitly chose this one, then explicitly asked to build everything initially flagged as not-yet-buildable. Same "propose scope, get confirmation" discipline as Disaster Monitoring.

**Real bug found and fixed on the live Supabase project before any new app code was written, since this workspace's real permission-gating feature is the first thing that would ever have exercised it:**

- `workspace_members.workspace_id` was typed `uuid`, but every workspace in this app is identified by a string slug ("agriculture", "government-ngos", etc.) — no `workspaces` table or slug→UUID mapping exists anywhere. Changed to `text`.
- `SupabaseAccountService.getWorkspaceMemberships()` has always selected a `scoped_resource_ids` column that was never created in the original identity-foundation migration — a real, live, previously-undetected bug (nothing had ever called this method before). Added the missing column.
- Table was confirmed empty (0 rows) before this migration — zero data-migration risk. Verified end to end afterward: inserted a real test row, read it back, deleted it, confirmed the table returned to empty.

- [x] **G.1** `apps/web/lib/account.ts` — `getAccountService()`, this app's first real consumer of `AccountService`/`SupabaseAccountService` (interfaces existed since Stage 3.3, never actually called by anything until now). Mirrors `getAuthService()`'s pattern.
- [x] **G.2** `apps/web/lib/get-workspace-role.ts` — `getWorkspaceRole()`, this app's first real role-based UI gating. **Fails safe to `viewer_external` (least privilege) on any missing session, missing membership, or lookup error** — stated as a deliberate, explicit default, not an accidental one, since every real user currently has zero membership rows anywhere (no invite/admin UI exists yet to create them).
- [x] **G.3** `apps/web/app/workspaces/government-ngos/` — `page.tsx` (Jurisdiction Overview: reuses `WeatherStatusProvider` + `SoilMoistureStatusProvider`, already-proven capabilities, for one demo point — genuine multi-domain synthesis without introducing new AI risk), `map/`, `workspace-shell.tsx` (first shell to display the signed-in user's actual role, mapped onto PRD A.10's specific role names).
- [x] **G.4** `apps/web/app/workspaces/government-ngos/report/page.tsx` — the formal report export. **Enforces `can(role, "reports:create")` on the report page itself, server-side — not just a hidden link on the home page**, matching `requireSession()`'s own "gate every route that matters, not just the link to it" principle, applied here to authorization for the first time. Print-optimized via a scoped stylesheet and `print-button.tsx`'s `window.print()` — the browser's native Print → Save as PDF, deliberately not a new server-side PDF-generation dependency this app has never used before.
- [x] **G.5** `packages/ui-components/src/layout/AppShell.tsx` — added a `data-app-shell-chrome` attribute wrapping Header/Sidebar/AIPanel, purely so the report page's print stylesheet can hide app chrome. Purely additive: confirmed zero behavioral change against `layout.test.tsx`'s existing assertions (all ARIA-role-based, none depend on exact DOM nesting) — 185/185 tests unchanged.
- [x] **G.6** Created a real `admin_owner` membership row for the owner's account in `government-ngos` (via direct SQL, the only way to create one today — no invite/admin UI exists), so the gated report-export flow is actually visible and testable, not just theoretically gated.
- [x] **G.7** Added to `workspace-nav.ts` and `dashboard-view.tsx`, same as every prior workspace.

**Honest scope, stated plainly — this covers a fraction of PRD A.10's actual ambition:**

- **Real jurisdiction boundaries**: NOT built — this app has no GIS polygon data anywhere. The demo location is a single point, same honest limitation as Logistics/Disaster Monitoring's own single-point scope.
- **Institutional permission tiers**: real, wired for the first time — but genuinely incomplete: no invite flow or admin console exists to assign memberships to anyone else; every other real user defaults to `viewer_external` until a row is created directly in Supabase.
- **Cross-agency collaboration features, custom scenario modeling, donor-specific report templates**: not built, PRD-only.
- **AI-generated narrative synthesis of the two domains into prose**: deliberately not built — the two capabilities are shown side by side, not blended into a single AI-written paragraph, to avoid claiming a synthesis capability beyond what's actually built.

**Fully verified:** production `next build` succeeded (all 22 routes, including all three new Government & NGOs routes; Middleware bundle unaffected). 185/185 existing tests passing (including `layout.test.tsx`, confirming the `AppShell` change is safe), clean lint, clean format. The `workspace_members` schema fix and its two new columns' behavior **were** verified directly against the live Supabase project (insert/read/delete a real row) before any code was written to depend on them — unlike most of this app's other network-dependent code paths.

**Not verified against the live NASA POWER API from this build environment** — same caveat as every other workspace's page.

---

## STAGE — INSURANCE WORKSPACE (10th workspace, fifth of the six previously-unbuilt)

Owner confirmed the last-built workspace (Government & NGOs) was complete and in accordance with the docs before authorizing this one. Same "propose scope, get confirmation" discipline as Disaster Monitoring and Government & NGOs, given PRD A.3's "near-audit-grade rigor" stakes.

**Real scope tension surfaced and resolved before any code was written:** PRD A.3's core AI value prop is a multi-hazard (flood/wildfire/storm/drought) AI-synthesized single risk score. This codebase has no interpretation capability for any of those four perils and no historical event archive to evaluate one against. Building it would mean fabricating a score — a direct violation of Constitution AI Principle #2 (never fabricate) and #4 (continuously evaluated against ground truth), the same reasoning Disaster Monitoring already used to decline multi-hazard AI cross-validation, applied here to a workspace with even higher real-world (financial/policyholder) stakes. Proposed and confirmed a narrower slice instead: reuse the two already-proven capabilities, decline the synthesized score explicitly and honestly in-page.

**Real reconciliation fix found and closed before adding new schema:** the live Supabase project has a tracked `fix_workspace_members_schema` migration (applied 2026-08-24, during the Government & NGOs build) with no corresponding file ever committed to `services/identity-service/supabase/migrations/` — a real drift between the repo and live reality that would not reproduce in a fresh environment. Added `0003_fix_workspace_members_schema.sql` as a pure documentation fix (no live change; the live column was already `text`).

- [x] **I.1** `services/identity-service/supabase/migrations/0004_audit_log.sql` — this app's first audit log (`audit_log` table, append-only, RLS on with a minimal "read your own rows" policy). Closes a gap `logger.ts` has flagged honestly since Stage 7 ("no admin/config mutation surface exists yet to audit") — Insurance is the first real one, per PRD A.3's explicit "audit-logged access" requirement. Deliberately a narrow, single-purpose table, not a generalized cross-app audit framework (Engineering Blueprint 4.5's "promote on genuine second consumer" applies). Applied via `Supabase:apply_migration` and verified against real inputs directly in SQL (insert → select → delete a real row) before any application code was written to depend on it, same discipline as every prior live-infra change.
- [x] **I.2** `services/identity-service/src/account.ts`/`index.ts` — `AuditLogEntry` type and `AccountService.recordAuditEvent()`/`SupabaseAccountService` implementation. Deliberately fails open: a failed audit write is logged via `logSecurity` but never blocks the underlying, already-authorized report generation — an audit trail that could accidentally deny legitimate access would be a worse outcome than one missing log line.
- [x] **I.3** `apps/web/lib/get-session-user-id.ts` — a small, new, dedicated helper for resolving the current session's userId, kept separate from `getWorkspaceRole()` (already consumed as `Promise<Role>` by three existing workspaces) rather than widening that function's return shape for the sake of this one new caller. Fails safe to `null`.
- [x] **I.4** `apps/web/app/workspaces/insurance/` — `page.tsx` (Underwriting Risk Context: reuses `WeatherStatusProvider` + `SoilMoistureStatusProvider` for one demo address, explicitly and honestly declining the PRD's multi-hazard synthesized score — see the scope-tension note above), `map/`, `workspace-shell.tsx` (PRD A.3's role names — Admin/Underwriter/Claims Adjuster/Analyst — mapped onto the existing four-role model, same honest-mapping approach `government-ngos/workspace-shell.tsx` used).
- [x] **I.5** `apps/web/app/workspaces/insurance/report/page.tsx` — the auditable report. Enforces `can(role, "reports:create")` server-side on the route itself, same as Government & NGOs. **Every successful report generation writes one real `audit_log` row** (`report:generated`) — the first page in this app whose access is actually logged, not just labeled auditable. Both a PDF path (`print-button.tsx`, browser print, same as Government & NGOs) and a real client-side CSV export (`csv-export-button.tsx`, same `Blob`/`download` pattern `research/dataset-explorer.tsx` already established) — PRD A.3 names both formats explicitly.
- [x] **I.6** Created a real `admin_owner` membership row for the owner's account in `insurance` (direct SQL, same as Government & NGOs — no invite/admin UI exists yet).
- [x] **I.7** Added to `workspace-nav.ts` and `dashboard-view.tsx`, same as every prior workspace. Fixed a stale doc comment in `apps/web/app/workspaces/layout.tsx` ("gates all five workspace home pages") found while in this area — it gates all of them, count no longer meaningful to state.

**Honest scope, stated plainly — this covers a narrow slice of PRD A.3's actual ambition:**

- **No multi-hazard AI-synthesized risk score** — the PRD's own headline AI feature for this workspace. Declined explicitly, not silently, per the reasoning above. Individual hazard signals only.
- **No portfolio, no multi-address upload, no policy data model** — one demo address, same limitation as every other narrow-slice workspace.
- **No claims-verification-against-historical-data tool** — needs a historical event archive this app doesn't have.
- **Institutional permission tiers**: real, but genuinely incomplete in the same way as Government & NGOs — no invite flow or admin console exists, every other real user defaults to `viewer_external`; Claims Adjuster's resource-level scoping is wired but inert (same pre-existing gap as `scoped_field_user` everywhere else in this app).
- **Parametric-trigger API, reinsurance stress-testing, climate-scenario portfolio modeling, white-labeled policyholder reports** — all explicitly named in the PRD as this workspace's own future premium features, not core scope.

**Fully verified:** production `next build` succeeded (all 25 routes, including all three new Insurance routes; Middleware bundle unaffected). 185/185 existing tests passing (unchanged — no new interpretation logic to test, by design), clean lint, clean format. A real type error (`unableToAnswer`'s actual `{ reason: string } | undefined` shape, not `boolean`) was caught by the real `next build`'s type-checking pass — not by the faster repo-wide `tsc --build` — and fixed before proceeding, the exact reason this project's workflow treats the real `next build` as non-optional. The `audit_log` table and the `insurance` `admin_owner` membership row **were** verified/created directly against the live Supabase project before any code depended on them.

**Not verified against the live NASA POWER API from this build environment** — same caveat as every other workspace's page.

---

## STAGE — EDUCATION WORKSPACE (11th and final PRD workspace)

Owner confirmed this scope explicitly before any code was written — the strictest scope-narrowing of any workspace yet, given PRD A.8's own "Special note": this workspace "requires the most conservative child-safety, data-privacy, and moderation design on the entire platform... as a hard requirement, not a later addition."

**Real scope decision, made before writing any code:** PRD A.8 calls for real Student accounts (capturing grade level at signup) and moderated peer collaboration between students. This codebase has no age/grade capture anywhere in auth, no COPPA-style verifiable-parental-consent flow, and no content-moderation infrastructure at all. Unlike every other narrow-slice workspace, there is no honest "narrow but real" version of *real student accounts + peer social features* — narrowing that specific pair of features means removing the safety part, not the feature part, since the risk lives in real minors' data and unmoderated peer interaction existing at all without safeguards actually built. Proposed and confirmed: an **educator-only** build with zero student accounts, zero collection of any minor's personal data, and zero peer/social features, rather than an unsafe approximation of the real thing.

- [x] **E.1** `apps/web/app/workspaces/education/lib/simplify-explanation.ts` — grade-band explanation *framing*, deliberately NOT a new AI/interpretation capability. Never rewrites the real, already-evaluated provider summary sentence itself (re-wording a real number for a younger grade band risks silently changing its meaning — Engineering Blueprint's highest-ranked risk); only varies the pedagogical framing (intro question, plain-language confidence phrase, discussion prompt) around that untouched sentence, by grade band (K–2, 3–5, 6–8, 9–12).
- [x] **E.2** `apps/web/app/workspaces/education/lib/generate-lesson-plan.ts` — template-based sample lesson plan composed from the same real, sourced data sentences (not free-text AI generation an evaluation framework would need to check). Contains no real student names or data anywhere — a starting-point document for an educator to adapt, not a record of any actual class.
- [x] **E.3** `apps/web/app/workspaces/education/` — `page.tsx` ("Explain This Data": reuses `WeatherStatusProvider` + `SoilMoistureStatusProvider` for one demo location, grade-band selector via `explain-this-data-client.tsx`), `map/` (simplified guided map, one plain-language toggle, no analytical controls — deliberately simpler than every other workspace's map per PRD A.8's own ask), `lesson-plan/` (gated by `can(role, "export:data")`, downloadable via the same `Blob`/`download` pattern already used in `insurance/report`), `workspace-shell.tsx` (Educator/Admin + Educator roles only, mapped onto the existing four-role model — no Student role exists in this build).
- [x] **E.4** Added to `workspace-nav.ts` and `dashboard-view.tsx`, same as every prior workspace. Created a real `admin_owner` membership row for the owner in `education` (direct SQL, same as Government & NGOs and Insurance).

**Honest scope, stated plainly:**

- **No real Student accounts anywhere** — no signup captures grade level, no student ever logs in, no student data is collected or stored. This is the deliberate scope decision above, not an oversight.
- **No moderated peer collaboration / classroom social features** — PRD A.8 requires this to be moderated per child-safety-appropriate design; this codebase has no moderation infrastructure. A separate, explicitly-scoped future decision, not approximated here.
- **No Institution Admin multi-classroom oversight, no assignment/deadline notifications, no student project exports** — all meaningless without real student accounts/classrooms, none of which exist in this build.
- **All 11 PRD workspaces have now had a real build pass** — 5 built to full narrow-slice depth in earlier stages (Agriculture, Weather & Climate, Construction, Renewable Energy, Research) plus Public Explorer, Logistics & Shipping, Disaster Monitoring, Government & NGOs, Insurance, and now Education. Every one of the six previously-deferred workspaces was built only after its own explicit scope confirmation — none were quietly approximated.

**Fully verified:** a real bug (two wrong relative import paths, one unused-import lint failure) was caught by the real production `next build`'s compile and lint passes — not the faster repo-wide `tsc --build`, which stayed clean throughout — and fixed before proceeding, the same lesson Insurance's `unableToAnswer` type error already reinforced: the real `next build` step is not optional. Final clean production `next build` (all 28 routes, including all three new Education routes). 185/185 existing tests passing (unchanged — no new interpretation logic, by design), clean lint, clean format.

**Not verified against the live NASA POWER API from this build environment** — same caveat as every other workspace's page.

---

## STAGE — TEAM/INVITE UI (Part A of the "scoped_field_user + no invite UI" gap-closing work)

Owner confirmed this scope explicitly before any code was written — the first change to touch shared auth infrastructure and an already-shipped page's sidebar (all 10 authenticated workspaces) rather than adding a new isolated workspace.

- [x] **T.1** `AuthService.inviteUser()` / `verifyInviteCallback()` — real Supabase invite email via `admin.inviteUserByEmail`, not a new email system. Intended workspace/role/resource-scope travels in the invited user's `user_metadata`, read back and eagerly cleared exactly once by `verifyInviteCallback` so a replayed invite link can never grant access twice. **Real bug caught by the real `tsc --build`** (not even needing the slower `next build` this time): `admin.updateUserById`'s options type uses `user_metadata`, not `data` — `data` is only valid for `inviteUserByEmail`'s options, a different call on the same API. Fixed before proceeding.
- [x] **T.2** `AccountService.createMembership()` / `listWorkspaceMembers()` / `removeMember()`. `removeMember` refuses to remove a workspace's last `admin_owner` — a real safety rail, not a UI convenience, verified directly against live data (see below).
- [x] **T.3** `/auth/callback`'s new `type=invite` branch — verifies, creates the real `workspace_members` row, records an audit event (fail-open, same as every other `recordAuditEvent` caller), then redirects to the **existing** `/reset-password` page so the invitee sets their own password — no new "set password" screen built, since an invite-derived session and a recovery-derived session need to do the exact same thing next.
- [x] **T.4** One new, generic route: `/workspaces/[workspaceId]/team` (`page.tsx`, `team-shell.tsx`, `team-client.tsx`, `team-actions.ts`). Next.js App Router supports this dynamic segment as a sibling to all 10 existing static workspace folders without touching any of them. Gated by the already-defined-but-previously-unused `workspace:manage_team` permission. **Real mistake caught and fixed during development, not by the build tooling**: an early version tried to pass a plain inline function from the server `page.tsx` directly into `AppShell` (a Client Component) — not serializable across that boundary. Fixed by giving this page its own minimal `TeamShell` Client Component that owns the AI-panel toggle state locally, the same pattern every other workspace's own `workspace-shell.tsx` already uses.
- [x] **T.5** Added a "Team" sidebar link to all 10 authenticated workspace shells (Agriculture, Weather & Climate, Construction, Renewable Energy, Research, Logistics & Shipping, Disaster Monitoring, Government & NGOs, Insurance, Education) — mechanical, repetitive edits, flagged as a good candidate for extracting into shared sidebar config later (Engineering Blueprint 4.5-style observation), not fixed now.
- [x] **T.6** Fixed a stale doc-comment claim in `get-workspace-role.ts` ("no invite flow exists anywhere") now that one does.

**Real, previously-undiscovered gap found and closed while verifying this stage, not part of the original plan:** `profiles` had **zero rows** in the live project this entire time — verified directly (0 rows against 1 real `auth.users` row) — because no trigger has ever existed to populate it on signup. `AccountService.getProfile()` (Stage 3.3) has therefore been silently broken for every real signup since it was written; nothing exercised it against live data until `listWorkspaceMembers`'s new join did, here, for the first time. This directly undermined the Team page's core value (showing member emails), so it was fixed at the root rather than only patching the symptom: `services/identity-service/supabase/migrations/0005_profiles_signup_trigger.sql` adds a real `handle_new_user()` trigger on `auth.users` (populates `profiles` on every future signup — magic link, password, Google OAuth, or an accepted team invite) plus a one-time backfill for the single existing real user. Applied live and verified: `profiles` now correctly returns the real user's email via the exact join `listWorkspaceMembers` performs.

**Fully verified:** `createMembership` (upsert-on-conflict), `listWorkspaceMembers`'s two-query join, and `removeMember`'s last-`admin_owner` refusal were each verified directly against live Supabase data using a disposable `verify-test-workspace` — created, upserted, role-changed, deleted, confirmed empty — since the app's own `SUPABASE_SERVICE_ROLE_KEY` isn't available outside Vercel's env config to exercise the TypeScript methods directly. `next build`, `tsc --build`, lint, and 185/185 existing tests all clean.

**Not verified: an actual invite email delivered to and clicked from a real inbox** — the one part of this flow that cannot be exercised without sending a real email to a real address from this environment. Verify this once, live, before relying on it for a real second user.

---

## STAGE — AGRICULTURE FIELDS (Part B of the "scoped_field_user + no invite UI" gap-closing work)

Owner confirmed this scope explicitly before any code was written (as part of the same "both parts, as two separate pushes" confirmation covering Part A above) — the first change to modify an already-shipped, tested page's actual shape (Agriculture's Field Overview, single hardcoded demo point → real multi-field list) rather than only adding new files.

- [x] **F.1** `services/identity-service/supabase/migrations/0006_agriculture_fields.sql` — the real resource type `roles.ts`'s own module doc comment named as the missing piece ("no sub-workspace resource type... exists anywhere"). Deliberately Agriculture-only, not a generic cross-workspace "resources" table (Engineering Blueprint 4.5: promote to shared only once a genuine second consumer exists — no other workspace's PRD language supports a comparable concept yet). Create + read only — editing/deleting a field is a real, explicitly-deferred gap, not required to make the scoping mechanism real for the first time. Seeded with the pre-existing demo field so existing users don't suddenly see an empty list. Applied and verified live before any app code depended on it.
- [x] **F.2** `AccountService.listFields()` / `createField()`. `apps/web/lib/get-workspace-membership.ts` — a new, separate helper returning the full `WorkspaceMembership` (role + `scopedResourceIds`), not a change to `getWorkspaceRole()`'s existing `Promise<Role>` signature already consumed by all 10 workspace pages. Same "one caller needs more than the others" reasoning `get-session-user-id.ts` already used.
- [x] **F.3** `apps/web/app/workspaces/agriculture/page.tsx` rewritten: **the first page in this app that actually calls `can()` with a real `resourceId`.** Every field is filtered through `can(role, "data:view", { resourceId: field.id, scopedResourceIds })` before being shown — closing the *product* gap `roles.ts` named, three months after the *mechanism* itself was built and left untested at Stage 7. Every field gets its own real weather + soil-moisture status, one `NasaPowerConnector` call per field (deliberately not one batched multi-location call — see the page's own doc comment for the real correctness reason: batched records would mix fields together with no way to re-attribute them, since the interpretation providers don't filter by location). `add-field-form.tsx` / `field-actions.ts` — a real "add a field" path, gated by `can(role, "data:edit")`.
- [x] **F.4** `/workspaces/[workspaceId]/team`'s invite form now shows a real field picker — checkboxes over Agriculture's actual fields — whenever the selected role is `scoped_field_user`, letting an invite's `scopedResourceIds` be populated with real, meaningful values for the first time ever. Every other workspace passes an empty field list and the picker simply doesn't render — no fabricated resource concept invented for workspaces that don't have one.

**Honest scope, stated plainly:**

- ~~No editing or deleting a field~~ — closed by the follow-up below.
- **No collaboration/commentary threads on fields, no season-end report, no CSV/PDF export for Agriculture** — all named in PRD A.1 but out of scope for what this stage set out to do (make resource-scoping real), same as before this stage.
- **Every workspace besides Agriculture still has `scoped_field_user` behaving workspace-wide** — this was always the stated, honest limit of this work: only Agriculture has PRD language supporting a real "field" concept to scope to.
- **The AI panel's headline summary shows only the first visible field's status**, not a genuine multi-field summary — a true multi-field AI panel would need real design work this stage wasn't scoped to do; each field's own full breakdown renders correctly inline in its own card regardless.

---

## STAGE — AGRICULTURE FIELDS FOLLOW-UP: EDIT/DELETE

Owner asked for the next thing in the docs immediately after Part B shipped — this stage's own explicitly-flagged deferred gap ("no editing or deleting a field") was the most direct, concrete next item.

- [x] **FE.1** `AccountService.updateField()` / `deleteField()`. No new migration needed — writes already go through the service-role client, same trust boundary as every other write in this app; `0006`'s migration only ever added a `select` policy, so no `update`/`delete` RLS policy exists for non-service-role clients either way.
- [x] **FE.2** `field-actions.ts` gained `updateFieldAction()` / `deleteFieldAction()` — **resource-scoped**, unlike `createFieldAction`. Uses `getWorkspaceMembership` (role + `scopedResourceIds`), not just `getWorkspaceRole`, so a `scoped_field_user` holding `data:edit` in general is still refused for a field outside their configured scope — the first time this app's resource-scoping mechanism gates a *write*, not just a read.
- [x] **FE.3** `field-manage-controls.tsx` — inline edit form + two-click delete confirmation ("Delete" → "Confirm Delete?"), consistent with this app's design system rather than a native `confirm()` dialog. Rendered per-field in `page.tsx`, shown only when that specific field's resource-scoped `can()` check passes.

**Honest, known edge case — now closed** (see "STAGE — AGRICULTURE FOLLOW-UP: REPORT/EXPORT" below): deleting a field now also removes any dangling reference to it from every membership's `scopedResourceIds`.

**Fully verified:** `tsc --build`, real production `next build` (all 28 routes), lint, 185/185 tests (unchanged) all clean. `updateField`/`deleteField`'s exact SQL shapes verified directly against live data via a disposable test field — inserted, renamed/moved, deleted, confirmed the table returned to exactly one row (the real seed field) — same reasoning as every other stage: this sandbox has no access to the app's own service-role key to exercise the TypeScript directly.

---**Fully verified:** `tsc --build`, real production `next build` (all 28 routes, Agriculture's page size grew from the new form/multi-field logic but nothing broke), lint, and 185/185 existing tests (unchanged — `can()`'s resource-scoping logic already had direct unit test coverage from Stage 7, confirmed still passing) all clean. `listFields`/`createField`'s exact SQL shapes verified directly against live Supabase data (insert, select matching the real query, delete, confirmed clean) — same reasoning as Part A: the app's own `SUPABASE_SERVICE_ROLE_KEY` isn't available in this sandbox to exercise the TypeScript directly.

**Not verified against the live NASA POWER API from this build environment** — same caveat as every other workspace's page, now applying per-field rather than once.

---

- ~~Second **data-provider** connector (a genuinely different provider — not yet done; Stage 10 validated the ingestion/interpretation boundary with a second _parameter_ through the _same_ provider, which is real evidence but not the same test as a structurally different provider's data shape)~~ **Closed by Stage 10 ticket 10.6** — `OpenMeteoConnector`, a structurally different provider (different API shape, different domain, different license terms). See `docs/data-provenance/open-meteo.md`.
- Mobile app (`apps/mobile`)
- Admin console (`apps/admin`)
- Billing/subscriptions (`services/billing-service`)
- Any workspace beyond the 11 named in the PRD — all 11 now have a real build pass (Agriculture, Weather & Climate, Construction, Renewable Energy, Research, Public Explorer, Logistics & Shipping, Disaster Monitoring, Government & NGOs, Insurance, Education)
- Third-Party Workspace Marketplace (PRD Section C.2 — explicitly a post-Phase-1 capability)
- ~~Any "AI Workforce" / multiple visible AI personas (PRD Amendment 3 — deferred pending evidence of need)~~ **Un-deferred by explicit owner decision** — see STAGE 9 above. Not a silent reversal: recorded here so the reasoning (the original deferral was "pending evidence of need," and that evidence still doesn't exist — this was a deliberate bet, not a data-driven trigger) stays visible.
- A Firebase identity backend alongside or instead of Supabase — raised by the owner (Stage 13 follow-up #3), clarified rather than built: this app's identity system is Supabase, established since Stage 3. Revisit only on an explicit, confirmed decision to migrate everything off Supabase, not as an incremental addition alongside it.
- ~~General server-side page-gating (requiring a valid session for `/dashboard`/workspace routes)~~ **Closed** — see "STAGE — SERVER-SIDE PAGE GATING" above.
- ~~Wiring `refreshSession()` into the page-gating path~~ **Closed** — see "STAGE — SILENT SESSION REFRESH (Middleware)" above.
- Knowledge Graph **Phase 2** (real cross-domain causal reasoning — providers consuming other providers' `InterpretationResult`s) — see ADR-0004. Revisit only once Phase 1 has been live long enough to show where the real chaining opportunities are.
- Telemetry aggregation, a dashboard, or any third-party analytics vendor — see Decision #015 and `logTelemetry`'s own doc comment. Needs a real cost + consent/privacy-notice decision not made here.
- An API registry (`/config/apis/`), feature flags, and a numbered error catalog — all raised in the same external review as telemetry/knowledge-graph, all judged premature for the current scale (two connectors, nothing built-but-hidden, existing categorized structured logging already covers what a small error catalog would). Not built — revisit only if the project's actual scale changes enough to need them, not proactively.

---

## STAGE — RENEWABLE ENERGY FOLLOW-UP: SOLAR IRRADIANCE

Owner said "build the next thing in our docs immediately" after the Agriculture Fields edit/delete follow-up shipped — this was the most directly, explicitly flagged concrete next item: Stage 13's own doc comment named it verbatim ("Solar needs an irradiance data source (NASA POWER does offer `ALLSKY_SFC_SW_DWN`, but no provider or UI consumes it yet — real, scoped follow-up work, not built here)").

- [x] **SI.1** `SolarIrradianceStatusProvider` (`services/interpretation-engine`), closing exactly that gap. Uses `NasaPowerConnector` with `community: "RE"` — NASA POWER's own grouping for this parameter — rather than reusing wind's `community: "AG"` call. Deliberately describes irradiance *level*, not generation output or capacity factor: this codebase has no real panel/inverter specification or installed-capacity data for any asset, so estimating actual kWh generated would be fabrication. Bands (`minimal`/`low`/`moderate`/`high`) are this project's own generic interpretation, explicitly not a cited standard — same discipline as every other threshold-based provider. Deliberately does not borrow wind's cut-in/rated/cut-out framing: solar panels have no analogous hard "cut-out" from excess irradiance, so applying that shape would misrepresent the physics.
- [x] **SI.2** 12 new tests (`__tests__/solar-irradiance-status-provider.test.ts`), mirroring `wind-generation-status-provider.test.ts`'s exact structure and coverage (band classification at each boundary, confidence scaling, `unableToAnswer` on no data, `evaluate()` against ground truth, capability rejection).
- [x] **SI.3** `renewable-energy/page.tsx` now fetches and displays solar irradiance alongside wind as a second "Current Status" card, sharing the same demo location. The "Generation Outlook" forecast widget remains wind-only — a solar forecast equivalent would mirror `WindGenerationOutlookProvider`'s pattern but wasn't part of what was flagged as the immediate follow-up, so it's named as a further, separately-scoped extension rather than silently expanded into.

**Honest scope, stated plainly:**

- **Irradiance level only, not generation output or capacity factor** — see SI.1.
- **Still no hydro** — needs streamflow/hydrological data this codebase has no connector for at all, unchanged by this addition. Renewable Energy is now two of the PRD's three named asset types (wind + solar), not three.
- **No solar forecast/outlook widget** — only wind has one.
- **No anomaly detection for either asset type** — both describe conditions, not a comparison against real generation output, which this codebase has no data feed for.

**Fully verified:** `tsc --build`, real production `next build` (all 28 routes, sizes essentially unchanged), lint, and the full test suite — now **197/197 passing** (185 existing + 12 new, confirmed individually) — all clean.

**Not verified against the live NASA POWER API from this build environment** — same caveat as every other workspace's page; `ALLSKY_SFC_SW_DWN` under `community: "RE"` specifically has not been checked against a real API response.

---

## STAGE — RENEWABLE ENERGY FOLLOW-UP: SOLAR OUTLOOK

Owner said to keep going after the solar irradiance status stage shipped. This stage's own honest-scope note ("no outlook (forecast) for solar") was the direct next item, closing the gap the moment it was created rather than leaving it open.

- [x] **SO.1** `OpenMeteoConnector` extended additively with Open-Meteo's `shortwave_radiation_sum` (daily total, MJ/m²) — same "no gap when absent" pattern already established for wind. **Converted to kWh/m²/day at ingestion** (÷3.6, the standard MJ→kWh conversion) so the normalized record is unit-identical to NASA POWER's `ALLSKY_SFC_SW_DWN` regardless of which connector produced it — the same unit-unification discipline `WS2M`'s explicit `wind_speed_unit=ms` request already established, applied here at the ingestion layer instead since Open-Meteo has no equivalent "give me kWh directly" request parameter. Per ADR-0002: ingestion hides provider-specific format, interpretation never sees it.
- [x] **SO.2** `SolarIrradianceOutlookProvider`, mirroring `WindGenerationOutlookProvider`'s exact pattern (lead-time-based confidence gradient, reuses `classifySolarIrradiance` so the outlook and current-status providers can never silently disagree about what a given value means).
- [x] **SO.3** 10 new tests: 2 on the connector (the MJ→kWh conversion, and "no gap when absent"), 8 on the outlook provider mirroring `wind-generation-outlook-provider.test.ts`'s exact structure and coverage.
- [x] **SO.4** `renewable-energy/page.tsx` now shows a second full-width "Generation Outlook — Solar" card alongside wind's, both fed by the same underlying `OpenMeteoConnector` call shape (two separate calls, not a shared-fetch refactor — kept consistent with this page's existing one-call-per-widget structure rather than restructuring it as a side effect of this addition).

**Honest scope, stated plainly:** same as the current-status stage — irradiance level only, not generation output; still no hydro; no anomaly detection for either asset type. The AI panel's headline summary continues to show wind's outlook only, not a combined wind+solar summary — same "one representative result, full detail lives on the page" reasoning as Agriculture's multi-field panel.

**Fully verified:** `tsc --build`, real production `next build` (all 28 routes), lint, and the full test suite — now **207/207 passing** (197 + 10 new, confirmed individually by name) — all clean.

**Not verified against the live Open-Meteo API from this build environment** — same caveat as every other forecast-based provider's page; the real MJ→kWh conversion has not been checked against a real API response, only against a fixture built from Open-Meteo's documented response shape.

---

## STAGE — AGRICULTURE FOLLOW-UP: REPORT/EXPORT

Owner said to keep going after the solar outlook stage shipped. Picked the next honest, well-scoped item from PRD A.1's own "Reports"/"Exports" lines, plus closed the one small known edge case still open from the Fields edit/delete stage while in this area.

**Real scope decision, same discipline as every prior report page:** PRD A.1 asks for a "season-end summary report (conditions vs. historical norms, key events)." This app has no historical archive for any field — no prior-season data exists anywhere. Building a "vs. historical norms" comparison would mean fabricating a baseline, the same reasoning Insurance and Disaster Monitoring already applied to their own declined features. Built instead: a real, honest **current-conditions snapshot** report — the same numbers `page.tsx` shows, never independently recomputed (both pull from the same extracted `getFieldStatus` function), exportable as CSV or print/PDF.

- [x] **AR.1** `field-status.ts` — `getFieldStatus` extracted from `page.tsx` into its own file so `report/page.tsx` reuses it without duplication (Engineering Blueprint 4.5: promote once a genuine second consumer exists). Both pages now structurally cannot disagree about a field's numbers.
- [x] **AR.2** `report/page.tsx` — gated by `can(role, "reports:create")`, checked on the route directly (same enforcement point as `insurance/report`/`government-ngos/report`). Field visibility within the report still respects the same resource-scoped `can(role, "data:view", { resourceId, scopedResourceIds })` filter `page.tsx` uses — `scoped_field_user` doesn't currently have `reports:create` at all, so this is defense-in-depth for a permission combination that doesn't exist yet, not dead code.
- [x] **AR.3** `report/csv-export-button.tsx` — same `Blob`/`download` pattern as `insurance/report`'s, adapted for multiple fields (one row per field per metric) rather than a fixed two-row shape. `report/print-button.tsx` — identical browser-print approach as every other report page.
- [x] **AR.4** Added "Field Report" to the sidebar (`workspace-shell.tsx`'s `activeKey` gained a `"report"` value) and a linked card on the home page, shown only when `can(role, "reports:create")`.
- [x] **AR.5** **Closed the one known edge case left open since the Fields edit/delete stage**: `AccountService.deleteField()` now also removes any dangling reference to the deleted field from every membership's `scopedResourceIds` (fetch-then-write, since the Supabase JS client's `.update()` sets literal values, not a computed `array_remove()` SQL expression — adding an RPC function felt like more new infra than this small cleanup warranted). Verified directly against live data: a real membership's scope was temporarily set to reference a disposable test field, the field was deleted through the exact same query shapes the code issues, and the dangling reference was confirmed removed — then the real membership was restored to its original state.

**Honest scope, stated plainly:**

- **Not a season-end summary, not a "vs. historical norms" comparison** — see the scope-decision note above. A current-conditions snapshot only.
- **No AI-generated narrative synthesis of the report** — each field's real interpretation results are shown as-is, not blended into prose.
- **Collaboration/commentary threads on fields** (PRD A.1) — still not built, unrelated to this stage's scope.

**Fully verified:** `tsc --build`, real production `next build` (all 29 routes, including the new `/workspaces/agriculture/report` route), lint, and the full test suite — **207/207 passing, unchanged** (no new interpretation logic, by design) — all clean. `deleteField`'s new cleanup logic verified directly against live Supabase data before shipping, same discipline as every prior live-data-touching change.

**Not verified against the live NASA POWER API from this build environment** — same caveat as every other workspace's page.

---

## CHANGE LOG

_(Add an entry each time this plan is meaningfully revised, so progress and re-scoping are traceable — this is the one piece of "process overhead" worth keeping even in execution mode.)_

- **v1** — Initial plan derived from Engineering Blueprint Section 18 and PRD Amendments 1–3.
- **v2** — Stage 8.1 marked complete via versioned GitHub Release on `main` (not a separate internal-only channel — see note above). Stage 8.2 marked in-progress, blocked on identifying beta testers.
- **v3** — Closed the flagged Stage 7 follow-up gap: added Next.js-specific ESLint rules (`core-web-vitals`) scoped to `apps/web`, via `@next/eslint-plugin-next`'s flat-config export rather than the legacy-format `eslint-config-next` package. See note under 7.3.
- **v4** — Worked through the remaining Stage 7 "not built, flagged honestly" list from `docs/reviews/stage-7-observability-security-accessibility.md`. Closed: a script-focused Content-Security-Policy (interim — `style-src` still permissive, see addendum), CI dependency-scanning (`pnpm audit`, informational for now pending an upstream Next.js patch — see `docs/security/known-vulnerabilities.md`), an incident-response runbook (`docs/runbooks/incident-response.md`), and a real threat model for the auth flow (`docs/security/auth-threat-model.md`). Investigated but deliberately left open: `scoped_field_user` resource-level scoping — no sub-workspace resource exists yet to scope against, so building scoping infrastructure now would be premature; revisit once a second workspace or per-resource data model exists. Full detail in the Stage 7 review's new "Post-Stage-7 Follow-Up" addendum.
- **v5** — Built the `scoped_field_user` resource-scoping _mechanism_ (not the product feature — see `roles.ts`'s updated doc comment): `can()` now takes an optional actor→action→resource scope, `WorkspaceMembership` gained `scopedResourceIds`, migration `0002_resource_scoped_membership.sql` adds the column. Fully tested, but inert until a real resource type exists. Owner rescoped Stage 8.2/8.3: no longer blocking further work. Owner also explicitly un-deferred PRD Amendment 3's "AI Workforce"/visible-persona item to build a Guide Character (Stage 9) — a deliberate, flagged deviation from the roadmap's own sequencing rules.
- **v6** — **Production incident and fix**: an earlier CSP (`script-src 'self'`, no `unsafe-inline`/nonce) blocked Next.js's own hydration scripts, causing a fully blank production page for every user. Root-caused against Next.js's official docs (also caught, before shipping a second time, that those docs describe Next 16's `proxy.ts` convention — this app is on Next 15.5.22, still `middleware.ts`). Fixed by reverting to the documented "Without Nonces" fallback (`script-src 'self' 'unsafe-inline'`); a full nonce-based CSP was deliberately not built mid-incident since it requires the entire app to give up static rendering — a real architectural tradeoff needing its own deliberate decision, not one made under outage pressure. Also derived the CSP's Supabase host from the real `SUPABASE_URL` env var instead of a manual placeholder, and fixed an unrelated bug found during the incident (`GuideCharacter.test.tsx` had landed in the wrong test folder, breaking every build) and a real pre-existing ESLint gap (`next.config.mjs` lacked Node globals, `process`/`URL` flagged as undefined — fixed via a new config-file override in `packages/config/eslint.config.js`).
- **v7** — Completed Stage 9 (Guide Character): first-use tutorial (`GuideTutorial`) and cross-page presence (Orbi added to `AppShell` itself, not per-page) — see Stage 9 above, now fully checked off. Started and substantially completed Stage 10 (Weather & Climate, second workspace) — `WeatherStatusProvider`, the `/workspaces/weather` route, and the dashboard card are done and verified (88/88 tests); the map view and forecast capability are honestly left open (10.5/10.6) rather than faked. See Stage 10 above for the full, real scope.
- **v8** — Stage 11: expanded sign-in beyond magic-link-only — password auth with a real, researched strength meter (`@zxcvbn-ts`), Google OAuth (required adding `@supabase/ssr`, a deliberate architecture decision confirmed with the owner first), a real Remember Me (required adding refresh-token handling that didn't exist before), and a full login-page redesign with an original SVG illustration. `docs/security/auth-threat-model.md` updated with the three genuinely new threats this adds, correcting an earlier line that had gone stale ("SSO/OAuth doesn't exist yet"). OAuth is code-complete and verified against real docs but not yet exercised against a live Google/Supabase setup — real manual console setup is still required before it can work at all. 94/94 tests passing.
- **v9** — Real production fixes found after v8 shipped: Google sign-in hung forever with `SUPABASE_ANON_KEY` unset (an uncaught error, not a handled one — fixed with proper try/catch placement, verified against Next.js's `redirect()`-inside-`try` gotcha); the login page allowed whole-page scroll; the tab-switcher between magic-link/password sign-in was a documented UX anti-pattern, replaced with a single primary flow. Also: the Agriculture map's tiles silently failed (a CSP `connect-src` gap — MapLibre fetches tiles via `fetch()`, not `<img>`, which only `img-src` covered).
- **v10** — Attempted a real 3D (WebGL) Orbi on the login page per owner request. Found and fixed two real bugs during verification (a bundle-isolation leak inflating every unrelated route by ~230kB; wrong React Three Fiber version would have shipped without checking React 18 compatibility first) — but a third, deeper bug (a documented class of React-instance-identity conflict between `react-reconciler` and pnpm's node_modules layout) caused a full production crash. Reverted to the working 2D Orbi rather than risk a fourth live fix without a real browser to test in, following this project's own incident-response principle (restore service first). `GuideCharacter3D` code kept in place with a clear status note for whenever this is revisited with real browser testing available.
- **v11** — Completed Stage 10 (10.5 map view, 10.6 real forecast) — see Stage 10 above, now fully checked off. The forecast closes the deferred-list "second data-provider connector" item too: `OpenMeteoConnector` is a structurally different provider from NASA POWER, not just a different parameter through the same one, validated via an additive, backward-compatible schema change (`recordType`/`forecastIssuedAt`) rather than a breaking one. Real licensing caveat found and documented: Open-Meteo's free tier is non-commercial-use only. 114/114 tests passing.
- **v12** — Stage 12: Construction, the third workspace. `ConstructionRiskStatusProvider` cross-references current temperature and wind speed against per-activity thresholds (concrete pour, crane operation, roofing work) for a go/caution/no-go recommendation, reusing `NasaPowerConnector` unchanged (a second parameter, `WS2M`, alongside `T2M`). Workspace route, dashboard card, and map view all mirror the existing two workspaces' structure. Honestly scoped: today's status only (no forecast-based Site Risk Timeline yet — needs wind data `OpenMeteoConnector` doesn't fetch), no excavation/flash-flood or lightning-proximity coverage (no precipitation or strike-detection data source exists), and fixed rather than project-configurable thresholds. 126/126 tests passing.
- **v13** — Stage 12 follow-up, same session: (1) the Site Risk Timeline, closing v12's own flagged gap — `ConstructionSiteRiskTimelineProvider`, plus an additive extension of `OpenMeteoConnector` to also fetch daily max wind speed (explicitly in m/s), and (2) a cross-workspace sidebar switcher (`WORKSPACE_LINKS`) shared by every workspace shell and the Home Dashboard. 137/137 tests passing.
- **v14** — Stage 13: Renewable Energy, the fourth workspace (wind only). `WindGenerationStatusProvider` (current) + `WindGenerationOutlookProvider` (forecast-based Asset Generation Outlook, the PRD's own named first-run widget) both classify wind speed into a generic turbine operating band (below-cut-in/ramping/rated-output/cut-out), reusing NASA POWER and Open-Meteo's existing wind pipelines unchanged. Insurance was considered and explicitly passed over given its PRD-stated "near-audit-grade rigor" requirement and larger scope. Honestly scoped: wind only (no solar/hydro), no anomaly detection against real output, generic not asset-specific turbine envelope. 158/158 tests passing.
- **v15** — Stage 13 follow-up, same session: app shell rework. `Sidebar` now renders grouped sections instead of one flat list, fixing the reported "collision" between the cross-workspace switcher and the current workspace's own pages. A shared `AppBrand` (placeholder logo + "World Vitality", linking to `/dashboard`) replaces each workspace's own non-clickable brand text. `AppShell` now owns a Ctrl+B/Cmd+B-toggleable, `localStorage`-persisted sidebar collapse — self-contained chrome behavior, not threaded through every page. 163/163 tests passing. The reported Google OAuth "continue to ...supabase.co" branding issue is external Google Cloud Console/Search Console configuration (and possibly Supabase's paid custom-domain feature) — not addressed in code; concrete steps were given directly to the owner instead.
- **v16** — Stage 13 follow-up #3: Forgot Password. Clarified with the owner that this app's identity system is Supabase (since Stage 3/11), not Firebase — built on Supabase rather than introducing a second, parallel user database. `requestPasswordReset`/`verifyPasswordResetCallback`/`updatePassword` added to `AuthService`; `/auth/callback` gained a `type=recovery` branch (redirects to `/reset-password`, not `/dashboard`, with `rememberMe` forced false); new `/forgot-password` and `/reset-password` pages; `updatePasswordAction` signs the user out immediately after a successful reset rather than leaving the recovery-derived session active. `docs/security/auth-threat-model.md` gained threat #9. Surfaced a real pre-existing gap while building this: no page in the app actually requires authentication server-side yet — recorded as open follow-up, not fixed here. 163/163 tests passing (no new unit-testable logic — this depends on a live Supabase project, same as every other `SupabaseAuthService` method).
- **v17** — Stage 14: Research, the fifth workspace — the Dataset Explorer. Chosen because its PRD design ("minimally interpreted, maximally transparent") is close to a direct match for what the ingestion layer's `NormalizedDataRecord`/`Provenance` already provide untouched — no new connector, no new `InterpretationProvider` at all, the one workspace shell in this app that deliberately has no AI interpretation panel. Real CSV export (client-side); map shows raw values, not a risk-band overlay, the one map in this app that isn't color-coded. Education, Government & NGOs, and Public Explorer were considered and passed over — Education specifically because its own PRD section demands "the most conservative child-safety design... as a hard requirement," not something to build casually alongside other tickets. 163/163 tests passing; one real accessibility fix made (an ESLint-flagged radio label, fixed with `aria-label`, not suppressed).
- **v18** — Stage 14 follow-up: evaluated an external AI review of the project (written without repo access) on its merits rather than at face value. Sorted its eight recommendations into "already done" (confidence scoring), "premature for current scale" (API registry, feature flags, error catalog — not built, said so directly), and "worth doing now" — this ticket. Built `packages/knowledge-graph` (ADR-0004, Decision #014) as an explicit two-phase plan, committing only to Phase 1 (metric → capability → workspace, derived from real provider wiring) and deferring the causal cross-domain reasoning the review described, since no provider in this codebase consumes another provider's output yet. Built telemetry (`logTelemetry`) on the existing structured-logging pipeline rather than a new vendor — real events (page views, dataset fetches, the full auth funnel), explicitly not an aggregation/dashboard/consent decision that wasn't made. Wrote `docs/Decision-Log.md` (15 real decisions from actual project history) and `docs/Glossary.md` (13 real terms as actually used in the code). 173/173 tests passing (10 new).
- **v19** — Stage 14 follow-up #2: docs upkeep. Fixed `docs/README.md`'s stale ADR count and missing entries; wrote `docs/architecture/system-overview.md` (the `architecture/` folder had been an unfulfilled placeholder since Stage 0 — a genuinely missing single-page current-state reference, not a speculative addition); updated `docs/runbooks/incident-response.md`'s Sign-in Broken playbook to actually cover Google OAuth and Forgot Password (previously magic-link-only, a real gap this session's own Reset Password email-template issue would have fallen through), broadened its SEV-2 examples and data-provider section beyond Agriculture/NASA-POWER-only, and added a triage step for the static-vs-dynamic-rendering bug class caught during the telemetry work. 173/173 tests passing (docs-only change).
- **v20** — Stage 14 follow-up #3: real logo. Owner provided the actual World Vitality mark (globe/leaf/ribbon "W" + wordmark + tagline); replaced every placeholder — `AppBrand`'s "WV" circular badge, `AuthIllustration`'s abstract green-gradient SVG scene (the "green square"), and the previously-unset favicon (`app/icon.png`) and Open Graph image. `AppBrand` uses `next/image` (correct, since it lives inside `apps/web`); `AuthIllustration` deliberately uses a plain `<img>` instead, since it lives in the framework-adjacent `packages/ui-components` and shouldn't couple its source to the consuming app's specific Image component. Accessibility correctly changed alongside the visual, not just cosmetically: the old decorative SVG was rightly `aria-hidden`; the real logo carries real meaning (its own tagline), so it now gets real `alt` text — 2 tests rewritten to match. Also caught and fixed a real `metadataBase` warning from the production build's own output. 173/173 tests passing (2 changed, 0 net new).
- **v21** — Stage 14 follow-up #4: real logo, second pass. Owner sent a screenshot showing the old placeholder still on the sign-in page — found two spots follow-up #3 missed: the landing page (`app/page.tsx`) still had the original hardcoded accent-color square, and `/login`'s form column (visible below 768px, where the illustration panel doesn't render) was text-only with no icon. Both fixed with the same real mark asset. 173/173 tests passing.
- **v22** — Server-side page gating. Closed the gap flagged since Stage 13 follow-up #3: `/dashboard` and every workspace route rendered for anyone who requested the URL, signed in or not. `apps/web/lib/require-session.ts` validates the `wv_session` cookie via the same `getSession()` call `updatePasswordAction` already used in production; `app/dashboard/layout.tsx` and `app/workspaces/layout.tsx` apply it to their whole route trees. Deliberately a Server Component check, not Middleware (Edge runtime, and this app has already been burned once by an untested framework-internals assumption — the 3D Orbi crash). Honest scope: `refreshSession()` isn't wired in, since Server Components can't set cookies during rendering — a "Remember Me" user whose access token expires gets redirected to `/login` rather than silently refreshed; recorded as new, separate follow-up work, not fixed here. 173/173 tests passing (no new unit-testable logic — same precedent as every other Supabase-dependent auth code path in this app).
- **v23** — Silent session refresh via Middleware. Closed the one gap v22 deliberately left open. `apps/web/middleware.ts` — this app's first-ever Middleware file — opportunistically refreshes an expired access-token cookie using the `wv_refresh` cookie, so a "Remember Me" user isn't asked to sign in again just because their short-lived access token expired between visits. Deliberately narrow: Middleware only attempts the refresh; `requireSession()` in each layout still makes the real authorization decision, unchanged. A failed refresh (revoked token, outage, unexpected incompatibility) falls through to the same `/login` redirect that already existed — a bug here degrades gracefully, never breaks or exposes a page. Verified Edge-runtime compatibility by reading `SupabaseAuthService.ts`'s actual call path (plain `fetch`-based, no Node-only APIs) and Next 15.5.22's real shipped `.d.ts` files for `NextResponse.next({ request })`/cookie APIs before writing anything, then confirmed for real: production `next build` compiled the Middleware bundle cleanly for the Edge runtime. 173/173 tests passing, clean lint/format.
- **v24** — Breached-password screening. Closed half of the "no rate limiting or breached-password screening" gap flagged since Stage 13 follow-up #3. `apps/web/lib/password-breach-check.ts` checks new passwords against Have I Been Pwned's k-anonymity range API (only 5 hex characters of the password's hash ever leave the app) and is wired into both sign-up and password reset, rejecting a breached password with a clear message. Fails open on any API/network error, logged, never blocking a legitimate user over an unrelated third-party outage. Rate limiting deliberately NOT built this pass — needs a persistent cross-invocation store, and the only already-trusted option (a new Supabase table) is a live-production infrastructure change not made without explicit owner sign-off; recorded as the next open piece of this gap, not dropped. Also surfaced and honestly documented a real, pre-existing gap this ticket didn't create: `apps/web` has never been wired into the repo's test runner at all (not part of the root `tsc --build` project-reference graph), so the new pure hash-matching logic — while written to be unit-testable — has no test that would actually execute; verified manually against Node's own `crypto` output and a realistic fixture instead of shipping a misleadingly-labeled test file that would silently never run. 173/173 existing tests still passing, clean lint/format, clean production `next build`.
- **v25** — Modern loading screen. Owner asked for an Instagram-style shimmer skeleton, referencing a framework ("Boneframer") that reads an app's CSS to auto-generate matching skeletons — searched twice, by name and by behavior, and no such library was found to exist; evaluated honestly rather than building blind or dismissing the ask outright. Delivered the same real goal a safer way instead, confirmed with the owner first: `packages/ui-components/src/layout/AppShellSkeleton.tsx`, a full-page `AppShell`-shaped shimmer built from `Skeleton` (existed since Stage 5.2 but was unused anywhere in the app) and the same design tokens every other component draws from, so it's automatically theme/dark-mode-correct with zero new dependency risk. Wired in via `app/dashboard/loading.tsx` and `app/workspaces/loading.tsx` — Next.js's own built-in loading-UI convention, one file each covering all six top-level authenticated pages. 173/173 tests passing, clean lint/format, clean production `next build` (all 19 routes, bundle sizes essentially unchanged).
- **v26** — Sign-in rate limiting. Closed the last piece of the "no rate limiting or breached-password screening" gap, with the owner's explicit go-ahead for the live-infrastructure step it required. Confirmed the connected Supabase project first (exactly one, matching the live app) and inspected existing schema/RLS conventions before designing anything new. Applied a real migration to the live project (`auth_rate_limits` table, RLS enabled with zero policies — service-role only, matching every other identity table; three `security definer` RPC functions for lockout check/record-failure/record-success, sliding window: 5 failures in 15 minutes locks an account for 15 minutes) — verified directly against real inputs in SQL (simulated lockout, simulated clear-on-success, test rows cleaned up) _before_ writing any application code that depends on it. `AuthService`/`SupabaseAuthService` gained `checkSignInLockout`/`recordFailedSignIn`/`recordSuccessfulSignIn`, kept separate from `signInWithPassword` so that method's contract stays uncoupled from the lockout policy. `signInWithPasswordAction` checks lockout before ever calling Supabase's own sign-in API. Per-email only, not per-IP — stated honestly as a real, still-open scope boundary in both this file and `docs/security/auth-threat-model.md`, not silently unmentioned. 173/173 tests passing, clean lint/format, clean production `next build`. Unlike most auth code in this app, the actual database logic here was verified against the real, live Supabase project directly, not just compiled.
- **v27** — Public Explorer, the sixth workspace — the first of the six previously-unbuilt PRD workspaces, chosen because the PRD's own Open Questions section ranks it (alongside Education) as the platform's lowest-stakes workspace and separately recommends dedicated investment in it as a public-trust asset, unlike Insurance/Disaster Monitoring/Government & NGOs' explicitly higher-stakes requirements or Education's child-safety design mandate. `apps/web/lib/geocode.ts` resolves free-text place names to coordinates via Open-Meteo's free Geocoding API (same trusted provider family as `OpenMeteoConnector`); `apps/web/lib/explore-actions.ts` reuses the exact same, already-proven `NasaPowerConnector`/`WeatherStatusProvider` pair the Weather & Climate workspace uses, driven by the geocoded location instead of a hardcoded demo one — a further real test of ADR-0002's ingestion/interpretation boundary, not a shortcut. `app/explore/` is deliberately outside both `app/workspaces/` and `app/dashboard/`'s `requireSession()` gates, since PRD A.11 requires genuine anonymous access — the one page in this app intentionally reachable without signing in. Also fixed a real gap caught while shipping this: the landing page's only call to action was "Sign in," with no discoverable path to the anonymous experience at all — added an equal-weight "Explore the planet" button. Most of PRD A.11 (personalized story feed, delight-oriented map, story-card social exports, saved-location alerts, AI assistant chat) remains un-built, stated plainly, not glossed over. 173/173 tests passing, clean lint/format, clean production `next build` (`/explore` confirmed outside the Middleware matcher).
- **v28** — Logistics & Shipping, the seventh workspace — the second of the six previously-unbuilt PRD workspaces, chosen because it has no stakes disqualifier (unlike Insurance/Disaster Monitoring/Government & NGOs/Education) and needs no new architecture beyond the standard authenticated workspace pattern. `services/interpretation-engine/src/providers/logistics-route-risk-provider.ts` classifies `WS2M` wind speed into a route-disruption risk band (clear/elevated/high/severe), thresholds explicitly flagged as this project's own interpretation, not any port authority's official advisory — same honesty pattern as Construction's and Renewable Energy's providers. `apps/web/app/workspaces/logistics/` follows the identical structure every prior workspace already established (home page, map page, workspace shell), added to the cross-workspace sidebar and Home Dashboard tile grid. Honest scope: single hardcoded point, wind-only, current-conditions-only — none of PRD A.5's storm-track/port-status/multi-waypoint-route/forecast-outlook vision is built. 185/185 tests passing (12 new), clean lint/format, clean production `next build` (both new routes compile and remain correctly covered by the existing session gate — no new route-tree exception needed, unlike Public Explorer).
- **v29** — Per-IP sign-in lockout + password-reset rate limiting, closing the two remaining pieces flagged open after v26's per-email lockout. Owner-initiated go-ahead for the live-infrastructure step this required, same as v26. Applied a real migration (`rate_limiting_ip_and_password_reset`) directly to the live Supabase project: `auth_signin_ip_rate_limits` (20 failures/15 min locks an IP for 15 minutes, deliberately higher than the per-email threshold since an IP can represent many real users, and deliberately never cleared on success since one account succeeding doesn't prove a shared IP is safe) and `auth_password_reset_requests` (a 3-per-15-minute cooldown, not a lockout — a reset request has no "wrong password" outcome to distinguish). Both verified against real inputs directly in SQL before any application code was written to call them. `apps/web/lib/get-client-ip.ts` reads `x-forwarded-for`/`x-real-ip` for best-effort IP extraction inside a Server Action, returning `null` rather than a fabricated key when absent. `signInWithPasswordAction` now checks IP lockout before email lockout before ever calling Supabase's sign-in API; `requestPasswordResetAction` checks the reset-request cap before sending any email — verified this doesn't leak account existence, since the cap applies identically whether or not the email is real. 185/185 tests passing (unchanged — entirely RPC calls to a live database, same precedent as v26), clean lint/format, clean production `next build` (Middleware bundle unaffected, since the IP check runs inside the Server Action, not Middleware itself).
- **v30** — Disaster Monitoring, the eighth workspace — the third of the six previously-unbuilt PRD workspaces, and the first NOT built to the standard "narrow slice of the existing pattern" playbook without discussion first. Given the Constitution's own "zero tolerance"/"highest ethical weight" language for this specific workspace, a proposed scope was laid out and explicitly confirmed with the owner before any code was written, rather than quietly reusing the weather-threshold pattern every other workspace uses. `apps/web/lib/nws-alerts.ts` fetches live official alerts from `api.weather.gov` (the U.S. National Weather Service's free, no-key public API) — real government data relayed as-issued, deliberately with **no** AI summarization, scoring, or reinterpretation anywhere in this workspace (same honest-empty-AI-panel precedent Research established, applied here for a graver reason). Deliberately not a `DataIngestionConnector` — an alert is a structured event, not a numeric reading, same reasoning already applied to `geocode.ts`/`password-breach-check.ts`. `apps/web/app/workspaces/disaster-monitoring/` follows the established page/map/shell structure, with a permanent on-page disclaimer ("not a substitute for official guidance"). Honest scope: U.S. and territories only (NWS has no other coverage — hence a dedicated U.S. demo location, unlike every other workspace's shared Nigeria-based one); no fire-perimeter/flood-extent layers, evacuation-zone status, or shelter locations (need data partnerships this app doesn't have); no multi-hazard synthesis. 185/185 tests passing (unchanged — no interpretation provider exists here to test, by design), clean lint/format, clean production `next build` (both new routes compile and remain correctly covered by the existing session gate).
- **v31** — Disaster Monitoring follow-up: flood + shelter data. Owner asked to fill in the workspace's honestly-flagged gaps; researched what's actually free/no-key/official before building anything, same discipline as v30. `apps/web/lib/usgs-flood-impacts.ts` — the USGS Real-Time Flood Impact API, real currently-flooding infrastructure locations (endpoint shape confirmed from a real working third-party client's source, since this sandbox couldn't fetch USGS's own OpenAPI schema — parsing deliberately defensive about that uncertainty). `apps/web/lib/fema-shelters.ts` — FEMA/Red Cross designated shelter locations from HIFLD open data via the standard Esri ArcGIS REST spatial-query pattern, carrying FEMA's own operational-status caveat verbatim, unsoftened. Both added to `page.tsx` with the same honest empty/error states and zero-AI-interpretation design as Active Alerts. Explicitly declined, not just left unbuilt: multi-hazard AI cross-validation of the three sources into one risk verdict — doing so would contradict the workspace's own confirmed "relay, don't reinterpret" design and reintroduce the false-precision risk that design exists to prevent; the three sources are shown side by side instead, still zero synthesis. Fire-perimeter data (NASA FIRMS) remains genuinely unbuilt — the owner was given a step-by-step guide to obtain the required free API key, since this app cannot self-register for one. 185/185 tests passing (unchanged), clean lint/format, clean production `next build`.
- **v32** — Disaster Monitoring follow-up: fire detections. Owner registered for and provided a NASA FIRMS `MAP_KEY` following the v31 guide. `apps/web/lib/nasa-firms.ts` — `fetchActiveFireDetections()` using FIRMS's documented area API (`VIIRS_SNPP_NRT`, ~1° bounding box, last 24 hours). The key itself is never hardcoded anywhere in this repository — read only from `process.env.NASA_FIRMS_MAP_KEY`, which the owner must set in Vercel's environment variables (this agent has no tool to set Vercel env vars directly). Fails gracefully and distinctly on a missing key — a specific "not configured" state, not the generic error state, not a silently omitted section; confirmed the production build succeeds cleanly both with and without the variable set, since workspace pages are server-rendered on demand rather than evaluated at build time. Added as a fourth section on `page.tsx`, carrying `FIRE_DETECTION_CAVEAT` verbatim — these are satellite-detected thermal anomalies, not confirmed wildfires; agricultural burning and other heat sources can appear in the same feed. 185/185 tests passing (unchanged), clean lint/format, clean production `next build` (verified both with and without the key configured).
- **v33** — Government & NGOs, the ninth workspace — the fourth of the six previously-unbuilt PRD workspaces. Owner asked to build everything initially flagged as not-yet-buildable. Found and fixed two real, live bugs in `workspace_members` before writing any new code depending on it (`workspace_id` was typed `uuid` despite every workspace being identified by a string slug everywhere else in this app; `scoped_resource_ids`, a column `SupabaseAccountService` has always queried, was never actually created) — verified via a real insert/read/delete against the live table (confirmed empty first, zero data risk) before proceeding. `apps/web/lib/account.ts`/`get-workspace-role.ts` — this app's first real consumer of `AccountService` and first real role-based UI gating; fails safe to `viewer_external` on any missing session/membership/error, since no invite or admin UI exists anywhere yet to assign real memberships (created one real `admin_owner` row directly via SQL so the owner can see the gated flow work). `apps/web/app/workspaces/government-ngos/` — Jurisdiction Overview reusing already-proven `WeatherStatusProvider`/`SoilMoistureStatusProvider` for one demo point (genuine multi-domain synthesis, zero new AI risk), plus a formal report export enforcing `can(role, "reports:create")` on the report page itself, not just a hidden link — printed via the browser's native Print → Save as PDF rather than a new server-side PDF dependency. `packages/ui-components/src/layout/AppShell.tsx` gained a purely-additive `data-app-shell-chrome` attribute so the report's print stylesheet can hide app chrome, confirmed against `layout.test.tsx`'s existing (DOM-nesting-independent) assertions. Honest scope: no real jurisdiction boundaries (single demo point, same limitation as Logistics/Disaster Monitoring), no invite/admin UI, no AI-generated narrative synthesis of the two domains (shown side by side, not blended into prose). 185/185 tests passing, clean lint/format, clean production `next build` (all 22 routes). The schema fix itself was verified directly against the live Supabase project, unlike most of this app's other network-dependent code.

- **v34** — Insurance, the tenth workspace — the fifth of the six previously-unbuilt PRD workspaces. Owner confirmed the prior workspace (Government & NGOs) checked out clean before authorizing this one. Surfaced and resolved a real scope tension before writing code: PRD A.3's headline AI feature is a multi-hazard (flood/wildfire/storm/drought) synthesized risk score, but this codebase has no interpretation capability for any of those perils and no historical archive to validate one against — building it would mean fabricating a score, violating Constitution AI Principle #2/#4 the same way Disaster Monitoring's declined multi-hazard synthesis already established, for a workspace with higher real-world stakes. Proposed and confirmed a narrower slice: reuse `WeatherStatusProvider`/`SoilMoistureStatusProvider` for one demo address, decline the synthesized score explicitly in-page. Also found and closed a real repo/live drift while verifying the prior workspace: a `fix_workspace_members_schema` migration was applied and tracked live in Supabase but never committed as a file — added the missing reconciliation migration (no live change, since the live column was already correct). Built this app's first audit log (`audit_log` table, migration `0004_audit_log.sql`, applied and verified against real inserted/read/deleted data before any code depended on it) and `AccountService.recordAuditEvent()` (fails open — a logging failure never blocks a legitimate report), directly satisfying PRD A.3's "audit-logged access" requirement. `apps/web/app/workspaces/insurance/` follows the established page/map/shell/report structure; the report page enforces `can(role, "reports:create")` server-side and writes a real audit-log row on every successful generation, with both a PDF (browser print) and a real client-side CSV export. PRD A.3's role names (Admin/Underwriter/Claims Adjuster/Analyst) mapped onto the existing four-role model, same honest-mapping approach as Government & NGOs. A real type error (`unableToAnswer`'s actual `{ reason: string } | undefined` shape) was caught by the real `next build`'s type check — not the faster repo-wide `tsc --build` — and fixed before proceeding, reconfirming why that step stays mandatory. 185/185 tests passing (unchanged), clean lint/format, clean production `next build` (all 25 routes). Created a real `admin_owner` membership row for the owner in `insurance`, same as Government & NGOs.
- **v35** — Education, the eleventh and final PRD workspace. Owner confirmed the proposed scope explicitly before any code was written, given PRD A.8's own "most conservative child-safety design... as a hard requirement" note. Made a real scope decision rather than narrowing the usual way: PRD A.8's real Student accounts + moderated peer collaboration have no honest "narrow but real" version in a codebase with no age/grade capture, no parental-consent flow, and no moderation infrastructure — narrowing those two features specifically would mean removing the safety part, not the feature part. Built educator-only instead, with zero student accounts and zero collection of any minor's personal data anywhere. `lib/simplify-explanation.ts` adds grade-band explanation framing around already-evaluated provider output without ever rewriting the real data sentence itself; `lib/generate-lesson-plan.ts` composes a template-based sample lesson plan from the same real, sourced data, containing no real student names or data. `apps/web/app/workspaces/education/` follows the established page/map/shell structure plus a `lesson-plan/` page gated by `can(role, "export:data")`. A real bug (two wrong relative import paths, one unused-import lint failure) was caught by the real production `next build`'s compile/lint passes, not the faster `tsc --build`, and fixed before proceeding — the same lesson Insurance's `unableToAnswer` type error already taught. 185/185 tests passing (unchanged), clean lint/format, clean production `next build` (all 28 routes). Created a real `admin_owner` membership row for the owner in `education`. All 11 PRD workspaces now have a real build pass.
- **v36** — Team/Invite UI, Part A of the "scoped_field_user resource-scoping + no invite UI" gap-closing work. Owner confirmed scope explicitly before any code was written, since this is the first change to touch shared auth infrastructure and edit an already-shipped page's sidebar (all 10 authenticated workspaces) rather than add a new isolated workspace. `AuthService.inviteUser()`/`verifyInviteCallback()` use Supabase's own `admin.inviteUserByEmail` — a real invite email, not a new email system — with the intended workspace/role/scope carried in the invited user's metadata and eagerly cleared on accept so a replayed link can't grant access twice. `/auth/callback` gained a `type=invite` branch that creates the real `workspace_members` row and reuses the *existing* `/reset-password` page rather than building a new "set password" screen. One new generic route, `/workspaces/[workspaceId]/team`, serves all 10 workspaces as a dynamic-segment sibling to their static folders — gated by the previously-defined-but-unused `workspace:manage_team` permission. `AccountService` gained `createMembership()`/`listWorkspaceMembers()`/`removeMember()`, the last of which refuses to remove a workspace's last `admin_owner`. Two real mistakes were caught and fixed during development (an `AdminUserAttributes` field-name error the real `tsc --build` caught; an attempt to pass a plain function from a Server Component into the Client Component `AppShell`, fixed by giving the Team page its own minimal `TeamShell`, same pattern every workspace's own shell already uses). Found and fixed a real, previously-undiscovered production gap while verifying this stage: `profiles` had zero rows in the live project — no signup trigger had ever existed — silently breaking `AccountService.getProfile()` for every real user since Stage 3.3; added a real trigger plus a one-time backfill (migration `0005_profiles_signup_trigger.sql`), verified live. `createMembership`/`listWorkspaceMembers`/`removeMember`'s last-admin-owner refusal were each verified directly against live Supabase data via a disposable test workspace, cleaned up after — the app's own service-role key isn't available in this environment to exercise the TypeScript directly. 185/185 tests passing (unchanged), clean lint/format, clean production `next build`. Not verified: an actual invite email delivered to and clicked from a real inbox — the one piece of this flow that can't be exercised without a real email address.
- **v37** — Agriculture Fields, Part B of the "scoped_field_user resource-scoping + no invite UI" gap-closing work. Owner confirmed this scope as part of the same confirmation covering Part A — the first change modifying an already-shipped page's actual shape (Agriculture's single hardcoded demo point → a real multi-field list) rather than only adding files. New migration `0006_agriculture_fields.sql` adds the real resource type `roles.ts`'s own module doc comment had flagged as missing, deliberately Agriculture-only (no other workspace's PRD language supports a comparable concept). Seeded with the pre-existing demo field for continuity; applied and verified live. `AccountService.listFields()`/`createField()`; a new `get-workspace-membership.ts` helper (full membership, not just role, kept separate from `getWorkspaceRole()` to avoid a breaking change to its 10 existing callers). Agriculture's home page is now the first page in this app that actually calls `can()` with a real `resourceId` — every field is filtered through real resource-scoping before being shown, closing the *product* gap left open since the *mechanism* was built untested at Stage 7. One `NasaPowerConnector` call per field, deliberately not batched (would silently mix fields' data together — the interpretation providers don't filter by location). A real "add a field" form, gated by `can(role, "data:edit")`. The Team page's invite form gained a real field picker, shown only for Agriculture's `scoped_field_user` role — the first invite ever able to carry a meaningful `scopedResourceIds` value. `tsc --build`, real production `next build`, lint, and 185/185 tests (unchanged, `can()`'s scoping logic already had direct unit coverage) all clean. `listFields`/`createField`'s SQL verified directly against live data, same reasoning as Part A.
- **v38** — Agriculture Fields follow-up: edit/delete. Owner asked for the next thing in the docs immediately after Part B shipped; this stage's own explicitly-flagged deferred gap was the direct next item. `AccountService.updateField()`/`deleteField()` — no new migration needed, writes already go through the service-role client. `field-actions.ts` gained `updateFieldAction()`/`deleteFieldAction()`, resource-scoped unlike `createFieldAction` — the first time this app's resource-scoping mechanism gates a write, not just a read, using `getWorkspaceMembership` rather than `getWorkspaceRole`. `field-manage-controls.tsx`: inline edit form, two-click delete confirmation matching this app's design system. `tsc --build`, real production `next build`, lint, and 185/185 tests all clean. `updateField`/`deleteField` verified directly against live data via a disposable test field. Known, honestly-flagged edge case left unfixed: deleting a field doesn't clean up any dangling reference in a membership's `scopedResourceIds`.
- **v39** — Renewable Energy follow-up: solar irradiance. Owner said to build the next thing in the docs immediately after the Agriculture Fields edit/delete follow-up shipped; Stage 13's own doc comment had named this gap verbatim. New `SolarIrradianceStatusProvider`, using `NasaPowerConnector` with `community: "RE"` (not wind's `"AG"`) for `ALLSKY_SFC_SW_DWN`. Deliberately describes irradiance level only, not generation output or capacity factor — this codebase has no panel/inverter spec or installed-capacity data for any asset, so estimating real kWh would be fabrication. Bands (minimal/low/moderate/high) are this project's own generic interpretation, explicitly not borrowing wind's cut-in/rated/cut-out framing since solar has no analogous hard cut-out. 12 new tests mirroring the wind provider's test structure exactly. `renewable-energy/page.tsx` now shows solar alongside wind as a second Current Status card; the forecast Generation Outlook widget remains wind-only, named as a further extension rather than silently expanded. `tsc --build`, real production `next build`, lint, and the full suite — now 197/197 passing (185 + 12 new, confirmed individually) — all clean.
- **v40** — Renewable Energy follow-up: solar outlook. Owner said to keep going after the solar irradiance status stage shipped; this stage's own honest-scope note ("no outlook for solar") was the direct next item. `OpenMeteoConnector` extended additively with `shortwave_radiation_sum` (daily MJ/m²), converted to kWh/m²/day at ingestion (÷3.6) so it's unit-identical to NASA POWER's `ALLSKY_SFC_SW_DWN` regardless of source — per ADR-0002, ingestion hides provider-specific format. New `SolarIrradianceOutlookProvider` mirrors `WindGenerationOutlookProvider`'s exact pattern, reusing `classifySolarIrradiance` so the outlook and current-status providers can never disagree. 10 new tests (2 connector, 8 provider) mirroring existing wind test structure exactly. `renewable-energy/page.tsx` gained a second full-width "Generation Outlook — Solar" card. `tsc --build`, real production `next build`, lint, and the full suite — now 207/207 passing (197 + 10 new, confirmed individually) — all clean. Not verified against the live Open-Meteo API from this environment; the MJ→kWh conversion is checked only against a documented fixture.
- **v41** — Agriculture follow-up: report/export, plus closed the dangling scopedResourceIds edge case. Owner said to keep going after the solar outlook stage shipped. PRD A.1 asks for a "season-end summary vs. historical norms" report; this app has no historical archive for any field, so building that comparison would mean fabricating a baseline — same reasoning Insurance/Disaster Monitoring already applied to their own declined features. Built instead: a real, honest current-conditions snapshot report, exportable as CSV or print/PDF. `field-status.ts` extracts `getFieldStatus` out of `page.tsx` so `report/page.tsx` reuses it without duplication — both pages structurally cannot disagree about a field's numbers. `report/page.tsx` gated by `can(role, "reports:create")`, field visibility still resource-scoped. Also closed the one known edge case left open since the Fields edit/delete stage: `AccountService.deleteField()` now cleans up any dangling reference in a membership's `scopedResourceIds` before deleting the field, verified directly against live data (a real membership's scope was temporarily pointed at a disposable test field, the field deleted through the exact code path, cleanup confirmed, then the real membership restored). `tsc --build`, real production `next build` (29 routes), lint, and the full suite — 207/207 passing, unchanged — all clean.
