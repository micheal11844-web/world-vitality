# WORLD VITALITY — BUILD PLAN

**Type:** Execution artifact, not a foundational document. This file is expected to change constantly — check items off, reorder, add tickets — without needing approval as if it were the Constitution, Engineering Blueprint, PRD, or Experience Blueprint.
**Purpose:** Translate the Engineering Blueprint's Section 18 roadmap into concrete, sequenced, checkable tickets, so execution work (in Claude Code or elsewhere) doesn't need to re-derive intent from the four foundational documents every session.
**Operating constraint:** No local terminal / command-line workflow, no VS Code. All code changes land via a GitHub-integrated tool; Vercel deploys automatically from GitHub on merge — never a manual build/deploy step.

---

## HOW TO USE THIS FILE

Each ticket below is scoped to be independently completable and reviewable. Work top to bottom — later tickets assume earlier ones are done. Check off `[ ]` → `[x]` as completed. If a ticket turns out to need splitting, split it here rather than starting a new planning document.

---

## STAGE 0 — REPOSITORY FOUNDATION

- [~] **0.1** Initialize the `world-vitality` GitHub repository (monorepo, per Engineering Blueprint Section 1). Scaffold built and committed locally (`git init` + initial commit); pushing to an actual GitHub org repo requires GitHub account access — see `docs/onboarding/repository-setup.md`.
- [x] **0.2** Create the top-level folder skeleton exactly as specified in Engineering Blueprint Section 2 (`apps/`, `services/`, `packages/`, `infra/`, `docs/`, `tools/`, `tests/`, `.github/`), with a placeholder `README.md` in each explaining its purpose (one paragraph, copied/adapted from the Blueprint's rationale).
- [x] **0.3** Move the four locked foundational documents into `docs/` (`docs/constitution/`, `docs/engineering-blueprint/`, plus `docs/prd/` and `docs/experience-blueprint/` for the other two locked docs) and this `BUILD_PLAN.md` into the repo root.
- [~] **0.4** Set up `.github/CODEOWNERS`, `PULL_REQUEST_TEMPLATE.md` (done), and branch protection on `main` (required review, required passing checks, no force-push) — per Engineering Blueprint Section 1.5 and 13. Branch protection is a GitHub repo setting and requires admin access to turn on — checklist in `docs/onboarding/repository-setup.md`.
- [x] **0.5** Connect the repository to Vercel for automatic deploy-on-merge to a staging environment; confirm a trivial change (e.g., README edit) triggers a successful deploy end-to-end before writing any application code. Requires Vercel account access — checklist in `docs/onboarding/repository-setup.md`. **Closed via v6**: added `public/index.html` + `vercel.json` so the deployment actually serves something instead of a 404.
- [x] **0.6** Set up linting/formatting/TypeScript configuration presets in `packages/config/`, applied repo-wide.
- [x] **0.7** Write ADR-0001 (Monorepo decision) and ADR-0002 (Ingestion/Interpretation/Presentation separation) into `docs/adr/`, formalizing decisions already made in the Engineering Blueprint, per its ADR format (Section 5). ADR-0003 (Core Interfaces) also included, ahead of Stage 1, since it was already drafted.

## STAGE 1 — CORE ABSTRACTIONS (before any feature work)

- [x] **1.1** Define the internal data-ingestion interface (`services/data-ingestion/`) — the contract any provider connector must implement. No provider wired in yet.
- [x] **1.2** Define the internal AI/interpretation-provider interface (`services/interpretation-engine/`) — the contract any AI model adapter must implement.
- [x] **1.3** Define the shared data schema (`packages/data-schemas/`) that ingestion normalizes into and interpretation consumes from.
- [ ] **1.4** Write ADR-0003 documenting both interfaces and why they exist (protects provider-agnosticism per Constitution Section 4).

## STAGE 2 — FIRST DATA PROVIDER + DATA PROVENANCE

- [x] **2.1** Implement the first concrete ingestion connector (NASA) against the Stage 1 interface.
- [x] **2.2** Document data provenance and licensing for this source in `docs/data-provenance/` (Constitution Section 24 requirement — not optional).
- [ ] **2.3** Validate the ingestion → schema pipeline with real data end-to-end (no interpretation logic yet). **Partial:** the parsing/normalization pipeline is validated by an automated test against a realistic POWER API response fixture (see `services/data-ingestion/README.md`). Not yet exercised against the live `power.larc.nasa.gov` endpoint — the environment this was built in has no outbound access to that host. Needs a real run (e.g. via Claude Code) to fully close out.

## STAGE 3 — IDENTITY & ACCESS FOUNDATION

- [x] **3.1** Implement authentication (magic link + optional SSO groundwork) via `services/identity-service/`. **Note:** implemented against Supabase Auth; not yet exercised against a live Supabase project — see `docs/onboarding/repository-setup.md`, Stage 3 section.
- [x] **3.2** Implement the core permission-role model shared across workspaces (Admin/Owner, Operational User, Scoped/Field User, Viewer/External — per PRD Section C.2). **Note:** the PRD's actual Section C.2 is the Third-Party Workspace Marketplace, not a role definition — see `services/identity-service/README.md` for the documentation-gap detail and the permission matrix's own interpretation basis.
- [x] **3.3** Implement account settings basics: profile, data export, account deletion (Constitution Privacy Principles — must exist before any real user data accumulates). **Note:** data export records the request but doesn't yet assemble cross-service export data (see `account.ts`).

## STAGE 4 — INTERPRETATION ENGINE v1 (NARROW SCOPE)

- [x] **4.1** Build one deliberately narrow interpretive capability end-to-end (recommend: Agriculture soil-moisture status — plain-language insight + confidence level) using the Stage 1 AI interface. **Note:** deliberately not ML — a transparent, threshold-based classifier over NASA POWER's `GWETROOT` parameter. Threshold bands are this build's own interpretation, not a cited agronomic standard — see the provider's doc comments.
- [x] **4.2** Build the evaluation framework (`packages/ai-evaluation/`) and validate this first capability against ground truth before it's user-facing. **Note:** ground-truth cases validate internal consistency with POWER's own documented GWETROOT scale, not independently-sourced field data (e.g. real soil sensors) — see `packages/ai-evaluation/README.md`.
- [x] **4.3** Build the shared "confidence/uncertainty" design-language component (per Experience Blueprint recommendation, Section 20 of the Experience Blueprint) in `packages/ui-components/` — used everywhere from day one, not retrofitted later. **Note:** built as framework-agnostic data/logic, not an actual React/Vue/etc. component — no frontend framework has been chosen yet (Stage 6). See `packages/ui-components/README.md`.

## STAGE 5 — DESIGN SYSTEM FOUNDATION

- [x] **5.1** Implement design tokens (`packages/design-tokens/`) — color, typography, spacing — per Experience Blueprint Section 13 principles (no hardcoded values in components). Includes real, computed WCAG AA contrast verification (10 passing checks), not just an intent comment.
- [x] **5.2** Implement core shared components in `packages/ui-components/`: Button, Card, Input, Modal, Typography, Table, Empty/Loading/Error/Success states, Skeleton loader. React + Next.js confirmed with the project owner. 63 passing tests using real rendering (`@testing-library/react` + jsdom).
- [x] **5.3** Implement dark mode / light mode support as first-class from the start. Fully separate token set per theme in `theme.css`, not a filter/invert.
- [x] **5.4** Implement the app-shell layout components (Header, Sidebar, AI Panel dock) per the Experience Blueprint's wireframe (Section 4 app shell). AI Panel's collapse behavior (reclaims width, doesn't just hide) is a tested constraint, not just styling.

## STAGE 6 — FIRST APPLICATION SURFACE

- [x] **6.1** Build `apps/web` skeleton: routing structure, app shell wired to design system, authentication flow wired to `identity-service`. **Note:** real Next.js App Router build verified clean (`next build`, 9 routes). Login → magic-link → `/auth/callback` → session cookie flow is fully wired against Stage 3's `identity-service`, including a real gap found and fixed while wiring it up: `Session` had no `accessToken` field, so nothing could actually persist a session — added it.
- [x] **6.2** Build the Home Dashboard shell (cross-workspace landing view, per PRD Section B.3). **Note:** honest partial — only one real workspace (Agriculture) exists to show, and no alerts data model exists yet, so B.3's "surfaces the most urgent item" behavior isn't implemented, just the correct shape for it. See the page's own doc comment.
- [x] **6.3** Build one full Workspace Home (recommend: Agriculture) using the dashboard widget grammar from Experience Blueprint Section 9 and its wireframe. **Note:** Status widget is real end-to-end (`NasaPowerConnector` → `SoilMoistureStatusProvider` → `ConfidenceBadge`, live on every request via `force-dynamic`). Map thumbnail links to a real map. Trend/Comparison/Alert-summary/Recent-reports/Team-activity widgets are honest `StateDisplay` empty states — no historical-comparison, alerts, reports, or team-activity data models exist yet; rendering fabricated chart data there would violate the same "never fabricate" principle the interpretation layer itself is held to.
- [x] **6.4** Build the Map view (base layers + one data overlay) per Experience Blueprint Section 11. **Note:** narrow scope per this ticket's own wording, not the full Section 11 spec (no timeline scrubber, drawing tools, search, bookmarks, or sharing — those are unbuilt). MapLibre GL + OpenStreetMap tiles chosen as a low-stakes, reversible tooling pick (no API key/vendor account required, unlike Mapbox) — not confirmed with the project owner, unlike the Supabase/React decisions, since it's cheap to swap later.
- [x] **6.5** Wire the AI Panel to the Stage 4 interpretation capability, including confidence display. **Note:** the panel shows real `InterpretationResult` + `ConfidenceBadge` for whatever page the user is on ("contextually aware," per Section 4) — but there's no actual conversational turn-taking yet (no chat UI, no follow-up question loop). `InterpretationRequest.userQuery` is defined but unused.

## STAGE 7 — OBSERVABILITY, SECURITY, ACCESSIBILITY GATES

- [x] **7.1** Implement logging/monitoring/alerting basics (Engineering Blueprint Section 12) alongside Stage 6 — not after. **Note:** structured logging built (`apps/web/lib/logger.ts`); monitoring dashboards/alerting/correlation IDs are not — no observability vendor chosen yet. Full detail: `docs/reviews/stage-7-observability-security-accessibility.md`.
- [x] **7.2** Run a security review pass on the full pipeline built so far (Engineering Blueprint Section 14). **Note:** found and fixed real issues — 25 dependency vulnerabilities (10 high, via Next.js 14; upgraded to 15.5.22, down to 5 all transitive to Next itself), missing security headers (added). No CSP yet (architectural tradeoff with inline styles, documented). Full detail in the review doc.
- [x] **7.3** Run an accessibility pass against Stage 5/6 components (Experience Blueprint Section 15) before any public exposure. **Note:** found and fixed two real gaps — the map had zero accessible description (violated Section 15's own stated example), and `Button`'s small size was under the 44px touch-target minimum. Scoped to what's actually built, not all of Section 15's broader (unbuilt-feature) requirements. Full detail in the review doc.

## STAGE 8 — CONTROLLED RELEASE

- [ ] **8.1** Deploy to an internal release channel first (Engineering Blueprint Section 13 staged rollout).
- [ ] **8.2** Expand to limited beta once internal validation passes.
- [ ] **8.3** Retrospective: update this BUILD_PLAN with Stage 9+ tickets based on what was actually learned — do not pre-plan Stage 9 in detail until Stage 8 is real.

---

## EXPLICITLY DEFERRED (do not start early)

- Second data-provider connector (validates abstraction — deliberately after Stage 8, per Engineering Blueprint roadmap ordering)
- Mobile app (`apps/mobile`)
- Admin console (`apps/admin`)
- Billing/subscriptions (`services/billing-service`)
- Any additional workspace beyond Agriculture
- Third-Party Workspace Marketplace (PRD Section C.2 — explicitly a post-Phase-1 capability)
- Any "AI Workforce" / multiple visible AI personas (PRD Amendment 3 — deferred pending evidence of need)

---

## CHANGE LOG

_(Add an entry each time this plan is meaningfully revised, so progress and re-scoping are traceable — this is the one piece of "process overhead" worth keeping even in execution mode.)_

- **v1** — Initial plan derived from Engineering Blueprint Section 18 and PRD Amendments 1–3.
- **v2** — Stage 0 executed: folder skeleton, folder READMEs, foundational docs + ADRs filed into `docs/`, CODEOWNERS, PR template, CI workflow, and `packages/config` lint/TypeScript presets all committed. 0.1, 0.4, 0.5 remain partially open pending GitHub/Vercel account-level actions documented in `docs/onboarding/repository-setup.md`.
- **v3** — Stage 1 executed: `packages/data-schemas` (shared normalized record + provenance + gap-reporting schema), `services/data-ingestion` (connector interface), and `services/interpretation-engine` (AI provider interface) all committed, per ADR-0003. Also fixed Stage 0's incomplete pnpm setup — added the missing `pnpm-workspace.yaml` (required for pnpm to resolve workspace packages; `package.json`'s `"workspaces"` field alone is npm/Yarn syntax and pnpm ignores it) and a real `pnpm-lock.yaml`, which is what was causing CI's `--frozen-lockfile` step to fail. No provider or model is implemented against the Stage 1 interfaces yet — that's Stage 2 and Stage 4.
- **v4** — Stage 2 executed: `NasaPowerConnector` (`services/data-ingestion/src/connectors/`) implemented against the Stage 1 ingestion interface, pulling daily point data from NASA's POWER API (Agroclimatology community, no API key required). Handles POWER's fill-value sentinel for missing data as an explicit `IngestionGap`, never a fabricated value. `docs/data-provenance/nasa-power.md` documents licensing (public domain) and known limitations (grid resolution, model- vs. sensor-derived) per Constitution Section 24. Added an automated test (`node --test`, wired into `pnpm run test` and CI) validating the parsing pipeline against a realistic response fixture — 2.3 (live end-to-end validation) is partial: not yet run against the real API from an environment with outbound network access.
- **v5** — Stage 3 executed: `services/identity-service/` built on Supabase (Postgres + built-in magic-link auth — confirmed with the project owner directly, since no backend was specified in source docs). `AuthService`/`SupabaseAuthService` (3.1), the `Role`/`Permission`/`ROLE_PERMISSIONS` model with full test coverage (3.2), and `AccountService`/`SupabaseAccountService` covering profile/workspace-membership/data-export/deletion (3.3). Postgres schema + RLS policies in `supabase/migrations/0001_identity_foundation.sql`. Flagged, not silently resolved: the PRD's role names appear in exactly one passing mention with no detailed permission matrix anywhere (BUILD_PLAN's "Section C.2" citation is actually the wrong section — that's the Marketplace); the matrix in `roles.ts` is this build's own reasonable interpretation, worth a product review. Supabase-backed code is written against Supabase's documented API but not yet run against a live project — none has been provisioned; `docs/onboarding/repository-setup.md` now has the provisioning checklist.
- **v6** — Closed out 0.5: added `public/index.html` (a minimal static placeholder — deployment pipeline confirmation only, not `apps/web`) and `vercel.json` pinning `outputDirectory` to `public/` so Vercel serves only that one file rather than the whole repo. Fixed `0001_identity_foundation.sql` to be safely re-runnable (`drop policy if exists` before each `create policy` — Postgres has no `create policy if not exists`, which is what caused a real "policy already exists" error on a second run). Also corrected several file-content and file-location errors introduced during manual GitHub upload in v3–v5 (wrong tsconfig content at repo root, `packages/data-schemas` package.json/README swapped with `data-ingestion`'s, `ingestion-gap.ts` duplicated, and all of `identity-service`'s source files uploaded flat instead of under `src/`) — this repo snapshot has all of those fixes already applied.
- **v7** — Stage 4 executed: `SoilMoistureStatusProvider` (`services/interpretation-engine/src/providers/`) — the first narrow interpretation capability (4.1), a transparent threshold classifier over NASA POWER's `GWETROOT` parameter, not ML. `packages/ai-evaluation` (4.2) — provider-agnostic ground-truth evaluation framework, with a 7-case suite validating the soil-moisture provider. `packages/ui-components` (4.3) — the confidence/uncertainty design language as framework-agnostic data/logic (no frontend framework chosen yet — Stage 6), calm-toned per Experience Blueprint Section 10, deliberately not using error/warning color semantics for low confidence. All 20 tests across the whole repo pass. Also corrected a real license error found while researching real POWER parameters: `docs/data-provenance/nasa-power.md` and the connector's `provenance.license` field said `"public-domain"`; POWER's own site states `CC BY 4.0` — fixed to `"CC-BY-4.0"` everywhere it appears. Separately, fixed a structural bug affecting every package built so far: every `package.json`'s `main`/`types`/`exports` pointed at TS source (`./src/index.ts`) instead of compiled output, which broke real cross-package imports at runtime the first time a test actually needed one (Stage 4's `ai-evaluation` importing `interpretation-engine`) — repointed all 6 packages at `./dist/index.js`/`./dist/index.d.ts` and added explicit TS project references so build ordering is correct.
- **v8** — Stage 5 executed: `packages/design-tokens` (5.1) — color/typography/spacing tokens per Experience Blueprint Section 13, with real computed WCAG AA contrast verification (10 passing checks against actual hex values, not a claim in a comment). React + Next.js confirmed with the project owner (5.2) — `packages/ui-components` now has real components (Button, Card, Input, Modal, Typography, Table, Skeleton, StateDisplay) plus the app shell (Header, Sidebar, AIPanel, AppShell — ticket 5.4), all with real `@testing-library/react` + jsdom tests (63 passing total), including genuine Modal focus-trap/Tab-wrapping/focus-return behavior, not just typed interfaces. Dark mode (5.3) is a fully separate token set in `theme.css`, not a filter/invert. New tooling: `tools/dom-test-setup.mjs` (jsdom global setup for `node --test`), `eslint-plugin-react-hooks`/`eslint-plugin-jsx-a11y`.
- **v9** — Stage 6 executed: `apps/web` built as a real Next.js App Router app — login → magic-link → `/auth/callback` → session-cookie flow wired to Stage 3's `identity-service` (6.1); Home Dashboard shell (6.2, honestly partial — see BUILD_PLAN note); Agriculture Workspace Home (6.3) actually calling `NasaPowerConnector` → `SoilMoistureStatusProvider` → `ConfidenceBadge` live on every request; Map view (6.4) with MapLibre GL + OSM tiles (a new, low-stakes reversible tooling pick, not confirmed with the owner like Supabase/React were) showing the one real data overlay; AI Panel (6.5) wired to show the real interpretation result and confidence for whatever page the user's on. Verified with a genuine `next build` (9 routes, correct static/dynamic split) from a completely clean install, not just `tsc`. Found and fixed several real bugs only a real build surfaces: root `tsconfig.json` had no `files: []` and was silently scanning `apps/web` (fixed); `apps/web`'s own relative imports needed extensionless paths, not the `.js`-suffixed convention used by the compiled packages; `Modal`/`Input` needed `"use client"`; a Route Handler had an invalid export; the agriculture pages were wrongly statically prerendered (would have baked in stale environmental data forever) — fixed with `force-dynamic`. Also fixed a real gap in Stage 3's `Session` type, found only once something tried to actually persist a session: no `accessToken` field existed to store. Replaced the Stage 0 `public/index.html` placeholder and its `vercel.json` entirely — `apps/web/vercel.json` + `apps/web/package.json`'s `build` script now handle the real deploy, but **this requires a one-time manual step**: Vercel's Project Settings → Root Directory must be set to `apps/web` (documented in `docs/onboarding/repository-setup.md`, Stage 6 section) — not yet done, so the live Vercel deployment will not reflect any of this until that setting is changed.
- **v10** — Auth flow debugged and fixed against the real, deployed production app (a real end-to-end validation this project hadn't had before — every prior "not yet exercised against a live project" caveat on identity-service is now resolved for the core sign-in path). Found and fixed, in order: Vercel project's Framework Preset was stuck at `null`/"Other" independent of Root Directory (dashboard setting, not a code fix); the original PKCE `code`-exchange flow (`AuthService.exchangeCodeForSession`) cannot work at all with this app's stateless-Server-Action architecture, since PKCE needs a `code_verifier` persisted between the request and the callback and none exists here — replaced with Supabase's `token_hash` verification (`AuthService.verifyMagicLinkCallback`), which Supabase's own SSR docs recommend for exactly this shape; Supabase's Magic Link email template needed customizing to link with `token_hash`/`type=email` instead of the default `{{ .ConfirmationURL }}` (documented in `docs/onboarding/repository-setup.md`); Supabase's default built-in email sender is rate-limited to 2/hour and not for production use — resolved by the project owner configuring custom SMTP via Resend; Supabase's **Site URL** setting (separate from the `SUPABASE_AUTH_REDIRECT_URL` env var) defaulted to `http://localhost:3000`, which the `{{ .SiteURL }}` email template variable resolves from. Temporary `[debug]` error-detail-to-UI surfacing (added mid-debugging to shortcut several rounds of Vercel-log-checking) has been reverted now that the flow is confirmed working — replaced with structured server-side logging (see v11/Stage 7) and safe generic user-facing messages.
- **v11** — Stage 7 executed. **7.1**: structured logging (`apps/web/lib/logger.ts`, application/security categories) wired into the auth flow, replacing raw `console.error`. **7.2**: real security review — `pnpm audit` found 25 vulnerabilities (10 high, nearly all in Next.js 14.2.35) — upgraded to Next.js 15.5.22, down to 5, all transitive to Next itself; added missing security headers (`X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, restrictive `Permissions-Policy`); confirmed via grep (not assumed) that the service-role-key code path is genuinely server-only. **7.3**: accessibility pass against Stage 5/6 — found and fixed two real gaps: the map had zero accessible description (a direct violation of Section 15's own example), and `Button`'s small size computed to ~31px tall, under the 44px touch-target minimum. Full findings, including everything _not_ built (monitoring/alerting, CSP, correlation IDs, and all of Section 15's requirements for features that don't exist yet) are in `docs/reviews/stage-7-observability-security-accessibility.md` — written as an honest audit record, not a checklist claiming completeness it doesn't have.
