# tools/

Internal developer productivity tooling: `scripts/`, `generators/`.

Separated from `services/` because nothing here is ever deployed to serve end users. Scaffolding generators (e.g., "new service," "new package") belong here once the repo has enough repeated structure to justify them — not before.
