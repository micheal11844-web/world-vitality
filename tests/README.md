# tests/

Cross-cutting test suites that span multiple apps/services and therefore don't belong to any single owner: currently `e2e/`.

Unit and integration tests live alongside the code they test, inside the relevant `apps/`, `services/`, or `packages/` folder — only true end-to-end, cross-application flows belong here (Engineering Blueprint, Section 10).
